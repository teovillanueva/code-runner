---
phase: 07-oss-release-deployment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [DOCS-01]

must_haves:
  truths:
    - "A push or PR triggers a CI workflow on ubuntu-latest with lint, go-unit, js, and contract-drift jobs."
    - "The js job runs the API vitest + contract node:test suites against a live redis service on the port the tests expect."
    - "The contract-drift job installs Go + node + go-jsonschema and runs make contract-check so schema/codegen drift fails CI."
    - "The lint job runs gofmt/go vet and pnpm -r typecheck."
    - "The workflow is valid YAML and does not collide with the existing abuse.yml gate."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Broader CI matrix: lint, go-unit, js (with redis service), contract-drift"
      contains: "contract-check"
      min_lines: 60
  key_links:
    - from: ".github/workflows/ci.yml js job"
      to: "apps/api/vitest.config.ts REDIS_URL"
      via: "redis service port matches the REDIS_URL/TEST_REDIS_URL the API tests read"
      pattern: "6380"
    - from: ".github/workflows/ci.yml contract-drift job"
      to: "Makefile contract-check"
      via: "make contract-check after installing go-jsonschema"
      pattern: "contract-check"
---

<objective>
Add `.github/workflows/ci.yml` — the broader CI matrix the user requested beyond the existing Phase 4 abuse gate: lint (gofmt/go vet + pnpm typecheck), go-unit (`go test ./...`), js (`pnpm -r test` with a live Redis service), and contract-drift (`make contract-check`). This makes the repo genuinely CI-guarded and contribution-ready.

Purpose: Support DOCS-01's "contribution-ready OSS release" claim with a real CI matrix; guard against Go/JS regressions and wire-contract drift on every push and PR.
Output: A valid, idiomatic GitHub Actions workflow that runs on push + pull_request on ubuntu-latest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Existing CI pattern + the exact test/redis wiring to mirror
@.github/workflows/abuse.yml
@Makefile
@package.json
@pnpm-workspace.yaml
@apps/api/vitest.config.ts
@apps/api/package.json
@packages/contract/package.json
@apps/stub/package.json

<interfaces>
<!-- Exact facts the CI must line up with. Extracted from the repo. -->

Existing abuse.yml (the pattern to follow):
- name: abuse; on push branches:[main] + pull_request + workflow_dispatch; concurrency group per ref; ubuntu-latest.
- redis service `redis:7` with ports `6381:6379`, healthcheck redis-cli ping; env TEST_REDIS_URL: redis://localhost:6381.
- actions/setup-go@v5 with go-version-file: go.mod, cache: true.
- This new workflow is SEPARATE (name: ci) and must NOT duplicate the abuse job; abuse.yml stays as-is.

Root scripts (package.json): pnpm-managed; packageManager pnpm@10.33.2; engines node >=22.
- `pnpm typecheck` = `pnpm -r typecheck`; `pnpm test` = `pnpm -r test`; `pnpm contract:check`.

Per-package test/typecheck (package.json files):
- apps/api: `test` = `vitest run`; `typecheck` = `tsc --noEmit`. Needs live Redis.
- packages/contract: `test` = node --experimental-strip-types --test 'test/**/*.ts'; `typecheck` = tsc --noEmit. No Redis.
- apps/stub: has `typecheck` only.

API test Redis wiring (apps/api/vitest.config.ts):
- REDIS_URL defaults to `process.env.REDIS_URL ?? process.env.TEST_REDIS_URL ?? "redis://localhost:6380"`.
- => CI redis service MUST map host port 6380 → container 6379 (or set REDIS_URL/TEST_REDIS_URL to whatever port is chosen). Use 6380 to match the test default so no extra env is needed, OR set REDIS_URL env explicitly. Pick ONE and keep it consistent.
- API tests also read EXECUTOR_API_TOKEN and LANGUAGES_DIR (vitest.config sets these; confirm by reading the config). LANGUAGES_DIR must point at the repo `languages/` dir if the API tests resolve manifests — check vitest.config and set the env if required.

Contract drift (Makefile contract-check + packages/contract package.json check):
- `make contract-check` runs `pnpm --filter @code-runner/contract generate` then `git diff --exit-code -- packages/contract/gen`.
- Generation needs node (pnpm) AND Go-side `go mod tidy` (the `contract` target runs `go mod tidy`); the drift check itself regenerates TS + zod. The Go struct generator is `omissis/go-jsonschema` installed via `make tools` (go install). So the contract-drift job needs: Go (setup-go), node + pnpm install, `make tools` (installs go-jsonschema on PATH), then `make contract-check`.

Lint:
- gofmt: `test -z "$(gofmt -l .)"` style check. go vet: `go vet ./...`.
- pnpm -r typecheck for the TS packages.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm the API test Redis port + required env from vitest config</name>
  <read_first>apps/api/vitest.config.ts, apps/api/test/execute.test.ts, apps/api/test/admission.test.ts, packages/contract/package.json, package.json</read_first>
  <action>
Read apps/api/vitest.config.ts in full and confirm: (a) the exact REDIS_URL / TEST_REDIS_URL default port (currently `redis://localhost:6380`), and (b) every other env var the config injects for the API tests (EXECUTOR_API_TOKEN, LANGUAGES_DIR, ENABLE_CHANNEL_AUTH, etc.) and whether any are required at runtime vs defaulted. Record the authoritative list of env vars + the redis port the `js` CI job must provide. Do NOT modify any test or config file — this task only establishes the facts the next task wires into ci.yml. Capture the findings in the SUMMARY so the ci.yml job is provably correct.
  </action>
  <verify>
    <automated>cd /Users/teovillanueva/code-runner && grep -nE 'REDIS_URL|TEST_REDIS_URL|6380|LANGUAGES_DIR|EXECUTOR_API_TOKEN' apps/api/vitest.config.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The redis port the API vitest suite expects is identified (grep shows the default).
    - The full set of env vars the vitest config provides is recorded for use in ci.yml.
    - No files modified in this task.
  </acceptance_criteria>
  <done>Authoritative redis port and required env list for the js CI job confirmed from vitest.config.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Author .github/workflows/ci.yml with lint, go-unit, js, contract-drift jobs</name>
  <read_first>.github/workflows/abuse.yml, Makefile, package.json, apps/api/vitest.config.ts, packages/contract/package.json</read_first>
  <action>
Create `.github/workflows/ci.yml` (use the Write tool). Author valid, idiomatic GitHub Actions YAML — it runs on GitHub when pushed, not locally, so be careful. Mirror abuse.yml conventions (checkout@v4, setup-go@v5 with go-version-file: go.mod + cache, redis service with healthcheck).

Workflow shape:
- `name: ci`
- `on: { push: { branches: [main] }, pull_request: {}, workflow_dispatch: {} }`
- `concurrency: { group: ci-${{ github.ref }}, cancel-in-progress: true }`
- jobs (all `runs-on: ubuntu-latest`):

  1. `lint`: setup-go (go.mod), then `gofmt -l .` failing if any output (e.g. run `test -z "$(gofmt -l .)"`), `go vet ./...`; then pnpm/Corepack setup (actions/setup-node@v4 with node-version 22 + cache pnpm, OR `corepack enable`), `pnpm install --frozen-lockfile`, `pnpm -r typecheck`.

  2. `go-unit`: setup-go (go.mod, cache), `go test ./...` (the Docker-free suite; do NOT add `-tags=docker/abuse/worker_integration`).

  3. `js`: a `redis` service using the port confirmed in Task 1 (default `6380:6379` to match the vitest REDIS_URL default) with a redis-cli ping healthcheck; setup-node 22 + pnpm via corepack; setup-go is NOT needed unless contract tests require it (they use node:test) — only add setup-go if the contract test suite needs generated Go; it does not. Steps: `pnpm install --frozen-lockfile`, then `pnpm -r test`. Provide the env the API vitest suite needs (from Task 1): REDIS_URL (or TEST_REDIS_URL) pointing at the service, EXECUTOR_API_TOKEN, LANGUAGES_DIR set to `${{ github.workspace }}/languages`, ENABLE_CHANNEL_AUTH as needed — only those Task 1 proved are required.

  4. `contract-drift`: setup-go (go.mod, cache) + setup-node 22 + pnpm; `pnpm install --frozen-lockfile`; `make tools` (installs omissis/go-jsonschema on PATH); `make contract-check` (regenerates + `git diff --exit-code`). This fails CI on any schema/codegen drift.

  Optionally add a `docker-integration` job ONLY if it can be expressed cleanly (ubuntu has Docker): builds `make python-image`, redis service on 6381, runs `make test-worker` or `make test-docker`. If it adds risk of flakiness, OMIT it and note in SUMMARY that the abuse.yml gate + go-unit already cover the critical paths. Prefer correctness over breadth.

Use Corepack for pnpm (`corepack enable` or actions/setup-node `cache: pnpm` with packageManager field) so the pnpm@10.33.2 from package.json is honored. Pin action versions (@v4/@v5). Ensure `pnpm install` uses `--frozen-lockfile`.

Do NOT touch abuse.yml. Do NOT use fenced README-style code — write the YAML file directly.
  </action>
  <verify>
    <automated>cd /Users/teovillanueva/code-runner && test -f .github/workflows/ci.yml && python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))" && for j in lint go-unit js contract-drift; do grep -qE "^[[:space:]]+${j}:" .github/workflows/ci.yml || { echo "MISSING job: $j"; exit 1; }; done && grep -q 'contract-check' .github/workflows/ci.yml && grep -q 'pnpm -r typecheck' .github/workflows/ci.yml && grep -q 'go test ./...' .github/workflows/ci.yml && grep -qE 'redis:7' .github/workflows/ci.yml && grep -qE '6380|6379' .github/workflows/ci.yml && grep -q 'go-jsonschema\|make tools' .github/workflows/ci.yml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` parses as valid YAML (python yaml.safe_load succeeds).
    - Jobs `lint`, `go-unit`, `js`, `contract-drift` all present.
    - js job declares a `redis:7` service on the port the API tests expect (matches Task 1 finding; grep for the port).
    - contract-drift job runs `make contract-check` and installs go-jsonschema (`make tools`).
    - lint runs `pnpm -r typecheck`; go-unit runs `go test ./...`.
    - abuse.yml is unchanged.
  </acceptance_criteria>
  <done>ci.yml authored with the four-job matrix, valid YAML, redis service wired to the API test port, and contract drift guarded; abuse.yml untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor PR → CI | Untrusted PR code runs in CI; mitigated by GitHub's fork-PR secret restrictions (no secrets needed by these jobs). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Tampering | Wire-contract drift slipping into main | mitigate | contract-drift job runs `make contract-check` (regenerate + git diff --exit-code) on every push/PR. |
| T-07-04 | Denial of service | CI flakiness from misconfigured redis port | mitigate | redis service port matched to the vitest REDIS_URL default (confirmed in Task 1) with a healthcheck gate. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | CI uses `pnpm install --frozen-lockfile` (lockfile-pinned) and `go install` of the pinned go-jsonschema via `make tools`; no unpinned package additions in this plan. |
</threat_model>

<verification>
- `.github/workflows/ci.yml` is valid YAML (yaml.safe_load).
- Four jobs present: lint, go-unit, js, contract-drift.
- redis service port matches the API vitest expectation.
- abuse.yml untouched.
</verification>

<success_criteria>
- On push/PR, CI runs lint + go-unit + js + contract-drift on ubuntu-latest.
- The js job exercises the API + contract test suites against a live Redis.
- Wire-contract drift fails CI.
- The repo is genuinely CI-guarded beyond the existing abuse gate (supports DOCS-01's contribution-ready release).
</success_criteria>

<output>
Create `.planning/phases/07-oss-release-deployment/07-02-SUMMARY.md` when done.
</output>
