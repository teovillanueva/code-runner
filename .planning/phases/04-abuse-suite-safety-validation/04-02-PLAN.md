---
phase: 04-abuse-suite-safety-validation
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - .github/workflows/abuse.yml
  - README.md
autonomous: true
requirements: [TEST-07, TEST-08]
must_haves:
  truths:
    - "A GitHub Actions workflow runs the abuse suite on ubuntu-latest (real Linux cgroup v2), proving real cgroup OOM/CPU behavior, not just macOS Docker Desktop (TEST-07)"
    - "The workflow sets up Go, builds the executor/python:3.12 image, starts redis, and runs `make abuse`"
    - "The repo encodes that the abuse suite gates the language fan-out (Phase 6): the workflow runs on PRs and a documented dependency note states fan-out must not merge unless abuse is green (TEST-08)"
  artifacts:
    - path: ".github/workflows/abuse.yml"
      provides: "Linux CI workflow running make abuse on ubuntu-latest with redis + python image"
      contains: "ubuntu-latest"
      min_lines: 30
    - path: "README.md"
      provides: "Documented gate note: abuse suite gates the language fan-out"
      contains: "abuse"
  key_links:
    - from: ".github/workflows/abuse.yml"
      to: "make abuse"
      via: "a workflow step that runs the abuse target on the Linux runner"
      pattern: "make abuse"
    - from: ".github/workflows/abuse.yml"
      to: "executor/python:3.12 + redis"
      via: "build-image step + redis service/container the suite connects to via TEST_REDIS_URL"
      pattern: "executor/python:3.12"
    - from: "README.md gate note"
      to: "Phase 6 language fan-out"
      via: "documented dependency that fan-out requires a green abuse run"
      pattern: "fan-?out"
---

<objective>
Wire the abuse suite as a Linux CI gate. Author a GitHub Actions workflow that runs the abuse suite on `ubuntu-latest` (real Linux cgroup v2) — setting up Go, building the `executor/python:3.12` image, starting redis, and running `make abuse` — so the suite exercises real cgroup OOM/CPU behavior rather than only macOS Docker Desktop (TEST-07). Encode that this suite gates the language fan-out: run it on pull requests and document the dependency that no new language merges unless the abuse run is green (TEST-08). Validate the workflow YAML on this machine (it cannot execute without a GitHub runner).

Purpose: The abuse suite is only a real safety guarantee if it runs where prod runs (Linux cgroup v2). This plan turns the green local suite from plan 01 into the standing gate that protects every future language.
Output: `.github/workflows/abuse.yml` (valid, lint-clean) and a README gate note tying Phase 6 fan-out to a green abuse run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-abuse-suite-safety-validation/04-CONTEXT.md
@.planning/phases/04-abuse-suite-safety-validation/04-01-PLAN.md

<read_first>
- `Makefile` — the `abuse` target (aligned in plan 01 to `go test -tags=abuse -timeout 600s ./internal/worker/... -run Abuse -v`) and the `python-image` target (`docker build -t executor/python:3.12 languages/python-3.12`). The workflow reuses these targets rather than re-spelling the commands.
- `internal/worker/integration_test.go` — `dialTestRedis` reads `TEST_REDIS_URL` and falls back to `redis://localhost:6381`; `requireDockerAndImage` requires `executor/python:3.12`. The CI job must make redis reachable at the URL the harness expects and build the image on the runner's Docker daemon BEFORE running the suite.
- `.planning/CLAUDE.md` / project notes — Go is pinned via `go.mod` (`go 1.26` + toolchain); the runner uses `actions/setup-go` with the version from go.mod. `ubuntu-latest` runners are cgroup v2 and have Docker preinstalled (the host daemon is reachable for the moby SDK over the default socket).
- `.planning/ROADMAP.md` Phase 6 section — the language fan-out that this gate protects (LANG-06/07/08); the README note references it.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Linux abuse CI workflow (.github/workflows/abuse.yml)</name>
  <files>.github/workflows/abuse.yml</files>
  <read_first>
Makefile (`abuse`, `python-image` targets), internal/worker/integration_test.go (`dialTestRedis` TEST_REDIS_URL / localhost:6381 fallback, `requireDockerAndImage` needs executor/python:3.12), go.mod (Go version for setup-go).
  </read_first>
  <action>
Create `.github/workflows/abuse.yml` defining a workflow named `abuse` triggered on `pull_request` and on `push` to the default branch (`main`), plus `workflow_dispatch` for manual runs. Define a single job `abuse` running on `runs-on: ubuntu-latest`. Steps, in order:
1. `actions/checkout` (v4).
2. `actions/setup-go` (v5) with `go-version-file: go.mod` so the toolchain matches the pinned version, and `cache: true`.
3. Start redis: use a job-level `services.redis` mapping `image: redis:7` with `ports: ["6381:6379"]` (mapping container 6379 to host 6381 so the harness default `localhost:6381` resolves without extra env), OR a step that runs `docker run -d -p 6381:6379 redis:7`. Prefer the `services:` block. Set a job `env: TEST_REDIS_URL: redis://localhost:6381` for explicitness.
4. Build the python image on the runner's Docker daemon: a step running `make python-image` (which `docker build -t executor/python:3.12 languages/python-3.12`) so `requireDockerAndImage` finds it.
5. Run the gate: a step named `Run abuse suite` running `make abuse`. Give the job a sane `timeout-minutes` (e.g. 20).
Add a top-of-file comment documenting that this job is the Linux cgroup-v2 safety gate (TEST-07) and that branch protection / the fan-out phase depends on it being green (TEST-08). Encode `concurrency` to cancel superseded runs on the same ref. Do NOT add lint/unit/contract-drift jobs here — those are Phase 7; this workflow is the abuse gate only (per CONTEXT deferred scope).
Use exact, currently-valid action major versions (checkout@v4, setup-go@v5). No fenced code outside the YAML file itself.
  </action>
  <verify>
    <automated>python3 -c "import sys,yaml; d=yaml.safe_load(open('.github/workflows/abuse.yml')); assert d['jobs']['abuse']['runs-on']=='ubuntu-latest'; steps=d['jobs']['abuse']['steps']; blob=open('.github/workflows/abuse.yml').read(); assert 'make abuse' in blob and 'executor/python:3.12' in blob or 'make python-image' in blob; assert 'redis' in blob.lower(); print('workflow valid:', len(steps), 'steps')"</automated>
  </verify>
  <acceptance_criteria>
- `.github/workflows/abuse.yml` parses as valid YAML (the python yaml.safe_load gate exits 0).
- The `abuse` job has `runs-on: ubuntu-latest`.
- The workflow builds the python image (`make python-image`), makes redis reachable (redis:7 service mapped to host 6381 with TEST_REDIS_URL set), and runs `make abuse` as a step.
- The workflow triggers on `pull_request` (so it gates merges) and on push to `main`.
- It uses `actions/checkout@v4` and `actions/setup-go@v5` with `go-version-file: go.mod`.
- No lint/unit/contract jobs are present (abuse gate only).
  </acceptance_criteria>
  <done>A valid `.github/workflows/abuse.yml` runs the abuse suite on ubuntu-latest with redis + the python image and `make abuse`, gating pull requests on real Linux cgroup v2 behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Encode the fan-out gate dependency (README note) + final YAML sanity</name>
  <files>README.md</files>
  <read_first>
.github/workflows/abuse.yml (from Task 1), .planning/ROADMAP.md Phase 6 (LANG-06/07/08 language fan-out), README.md (existing structure / quickstart from Phase 3).
  </read_first>
  <action>
Add a short "Safety gate" subsection to README.md (under or near the testing/CI section) that documents TEST-08: the abuse suite (`.github/workflows/abuse.yml`, run via `make abuse` on Linux cgroup v2 CI) is the safety gate that MUST be green before any new language is added in the language fan-out (Phase 6 — Rust/R/SQLite). State explicitly: (a) the workflow runs on every pull request; (b) adding a language reuses this same harness and the gate must pass for the new images; (c) it is a required check (note that the repo owner should enable it as a branch-protection required status check on `main`). Reference the local command `make abuse` and the prerequisites (Docker cgroup v2 + redis:7 + executor/python:3.12). Keep it to a focused paragraph or short bullet list — do not restate the whole API contract.

If README.md does not yet exist, create a minimal one with a top heading and this Safety gate section (the Phase 3 quickstart may already exist; if so, append the section rather than overwriting). Then re-run the YAML validity gate from Task 1 to confirm the workflow still parses after any incidental edits, and (if `actionlint` is available on PATH) run it as an extra lint pass — otherwise skip silently.
  </action>
  <verify>
    <automated>grep -niE 'abuse' README.md && grep -niE 'fan-?out|language' README.md && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/abuse.yml')); print('yaml ok')"</automated>
  </verify>
  <acceptance_criteria>
- README.md contains a Safety gate section mentioning the abuse suite, `make abuse`, the `.github/workflows/abuse.yml` Linux CI gate, and that it must be green before adding a language in the fan-out phase (grep matches `abuse` and `fan-out`/`language`).
- The note states the workflow runs on pull requests and should be a required status check on `main`.
- `.github/workflows/abuse.yml` still parses as valid YAML after edits.
- If `actionlint` is installed, it reports no errors on the workflow; if absent, the step is skipped without failing.
  </acceptance_criteria>
  <done>The README documents the abuse suite as the standing safety gate that blocks the language fan-out unless green, and the workflow remains valid — TEST-07 and TEST-08 are both satisfied and encoded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev macOS vs prod Linux | Docker Desktop (macOS) hides cgroup v1/v2 OOM/CPU divergence (PITFALLS Pitfall 12); the CI gate runs the suite on real Linux cgroup v2 to close this gap. |
| untrusted language image -> gate | Future language images (Phase 6) are unproven until the abuse gate passes for them; the gate is the enforcement point. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | a new language image bypassing safety checks | mitigate | TEST-08 gate: abuse workflow runs on PRs + documented required-check note so fan-out cannot merge without a green abuse run |
| T-04-06 | Repudiation | "safety verified" claimed only on macOS | mitigate | TEST-07: workflow runs on ubuntu-latest (real cgroup v2), proving prod-parity OOM/CPU behavior |
| T-04-SC | Tampering | GitHub Action versions / supply chain | accept | Pinned to major-version tags of first-party actions (checkout@v4, setup-go@v5); no third-party actions introduced. |
</threat_model>

<verification>
- `.github/workflows/abuse.yml` parses as valid YAML and defines an `ubuntu-latest` job that builds the python image, starts redis, and runs `make abuse`.
- The workflow triggers on pull requests (gate) and push to main.
- README documents the abuse suite as the fan-out safety gate (required check on main).
</verification>

<success_criteria>
- A valid Linux CI workflow runs the abuse suite on real cgroup v2 (TEST-07).
- The suite is encoded as the gate for the language fan-out — runs on PRs + documented required check (TEST-08).
</success_criteria>

<output>
Create `.planning/phases/04-abuse-suite-safety-validation/04-02-SUMMARY.md` when done.
</output>
