---
phase: 04-abuse-suite-safety-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/worker/abuse_test.go
  - Makefile
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06]
must_haves:
  truths:
    - "A fork-bomb job is contained by --pids-limit; the worker survives and processes a later job (TEST-01)"
    - "An OOM job is killed by the memory cap; the worker survives (TEST-02)"
    - "An infinite-loop job is killed by the wall clock with result.timedOut=true (TEST-03)"
    - "A stdin-blocked job is killed by the idle clock with result.idleTimedOut=true (TEST-04)"
    - "A job reading to EOF exits cleanly (exitCode 0) after stdin_close, NOT idle-timed-out (TEST-05)"
    - "A giant-output job reports result.truncated=true, the worker keeps draining and does not deadlock (TEST-06)"
    - "No container labeled code-runner.jobId survives after any abuse case (no leak)"
    - "`make abuse` runs the suite to green against real Docker cgroup v2 + redis on this machine"
  artifacts:
    - path: "internal/worker/abuse_test.go"
      provides: "Adversarial abuse suite driving 6 hostile jobs through the full worker path, guarded by //go:build abuse"
      contains: "//go:build abuse"
      min_lines: 250
    - path: "Makefile"
      provides: "abuse target aligned to the actual suite tag/path"
      contains: "tags=abuse"
  key_links:
    - from: "internal/worker/abuse_test.go"
      to: "worker.New + runnerPkg.NewDockerSocketRunner + publisher.NewForTest"
      via: "full worker path (Redis queue -> worker -> DockerSocketRunner + session clocks + recording publisher)"
      pattern: "worker\\.New\\("
    - from: "internal/worker/abuse_test.go"
      to: "wire.ResultEvent flags"
      via: "asserting TimedOut / IdleTimedOut / Truncated / ExitCode on the terminal result event"
      pattern: "ResultEvent"
    - from: "Makefile abuse target"
      to: "internal/worker abuse suite"
      via: "go test -tags=abuse -run Abuse ./internal/worker/..."
      pattern: "internal/worker"
---

<objective>
Build the adversarial abuse/safety suite that proves the sandbox guarantees by driving six real hostile Python jobs through the FULL worker path (Redis queue -> worker run loop -> DockerSocketRunner with hardening + three clocks -> recording publisher), asserting the published terminal `result` flags and that NO container leaks after each case. Then ACTUALLY RUN it to green on this machine (Docker cgroup v2 + redis + executor/python:3.12 are all present) and paste the real output.

Purpose: This suite is the verification backbone of the whole project — it is the empirical proof that fork bombs, OOM, infinite loops, idle stdin, EOF, and output floods are all contained and the worker survives. It gates the language fan-out (wired in plan 02).
Output: `internal/worker/abuse_test.go` (build tag `abuse`), an aligned `make abuse` target, and a green run with pasted output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-abuse-suite-safety-validation/04-CONTEXT.md

<read_first>
- `internal/worker/integration_test.go` — REUSE this harness wholesale. It already defines, behind `//go:build worker_integration`, exactly what the abuse suite needs: `dialTestRedis(t)`, `requireDockerAndImage(t)`, `assertNoContainerLeak(t, cli, jobID)`, `integrationSeccompProfilePath()`, the `integrationTriggerer` recording publisher with `waitFor`/`allEvents`, and the full stack-wiring pattern (jobstore.New, stdintransport.NewRedis, runnerPkg.NewDockerSocketRunner, publisher.NewForTest, worker.New, store.WriteSpec + store.Enqueue, run worker loop in goroutine, drive start/stdin over Redis Publish). The abuse suite mirrors this wiring per case. Copy the helper functions into abuse_test.go (or place the abuse tests in the same package so they compile together) — do NOT call into the worker_integration-tagged file from the abuse-tagged file (different build tags exclude each other).
- `internal/worker/worker.go` — the run loop. Note: the worker subscribes ctrl/stdin BEFORE publishing "queued"; the job parks at the start-handshake until a `start` ControlMessage on `ctrl:<jobId>`; `result.TimedOut` covers BOTH wall and CPU clock; `result.IdleTimedOut` is the idle clock; `result.Truncated` is the output cap. `Run` commands are passed as argv; inline `python -c "..."` avoids the read-only-rootfs file-copy path (the integration tests use this), while file injection via `wire.FileInput` + `["python","main.py"]` also works through the /workspace tmpfs.
- `internal/session/interactive.go` + `internal/runner/runner.go` (`Result` struct) — clock-to-flag semantics: wall/CPU -> `TimedOut`; idle -> `IdleTimedOut`; output cap -> `Truncated`; normal exit -> `ExitCode` set (0 for clean EOF exit).
- `packages/contract/gen/go/wire/wire.gen.go` — `ResultEvent` (ExitCode *int, Signal *string, TimedOut, IdleTimedOut, Truncated bool, DurationMs int), `ControlMessage{Type}` with `ControlTypeStart`/`ControlTypeStdinClose`/`ControlTypeKill`, `StageEvent`/`OutputChunkEvent`, `JobSpec`, `Limits`, `FileInput`.
- `internal/runner/docker_integration_test.go` — Phase 2 assertion style for the same abuse intents at the RUNNER level (TestIntegrationWallClock, TestIntegrationIdleClock, TestIntegrationCpuClock, TestIntegrationOutputTruncation, TestIntegrationTreeKillNoLeak). Phase 4 is the END-TO-END variant: same intents, but submitted as real jobs through the worker and asserted on the PUBLISHED result event, not the returned Result.
- `Makefile` — existing `abuse` target: `go test -tags=abuse -timeout 600s ./apps/worker/... -run Abuse -v`. The harness lives in `internal/worker`, so realign the path to `./internal/worker/...` (see Task 3).
- `.planning/research/PITFALLS.md` — the "Looks Done But Isn't" checklist (lines ~381-394) and the Pitfall 1 CPU-clock-evasion case are the source of the abuse cases.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Abuse suite scaffolding + containment cases (fork bomb, OOM, infinite loop, CPU-evasion)</name>
  <files>internal/worker/abuse_test.go</files>
  <read_first>
internal/worker/integration_test.go (harness helpers + stack wiring), internal/worker/worker.go (start-handshake, result flags), packages/contract/gen/go/wire/wire.gen.go (ResultEvent, ControlMessage, JobSpec, Limits), .planning/research/PITFALLS.md (Pitfall 1, "Looks Done But Isn't" checklist).
  </read_first>
  <action>
Create `internal/worker/abuse_test.go` in `package worker_test` with the build constraint `//go:build abuse` on the first line. Copy the harness helpers from integration_test.go into this file (they are excluded by build tag, so re-declare them here under the abuse tag): `dialTestRedis`, `requireDockerAndImage`, `assertNoContainerLeak`, `integrationSeccompProfilePath`, and the `integrationTriggerer` recording-publisher type with `Trigger`/`waitFor`/`allEvents`. Add a single shared `driveAbuseJob` helper that, given a `*testing.T`, a `wire.JobSpec`, and an optional post-start interaction callback, wires the full stack (jobstore.New, stdintransport.NewRedis, NewDockerSocketRunner with the seccomp path, publisher.NewForTest(triggerer), worker.New with MaxSandboxes 2 / WarmupMs 10000 / ClaimTimeout 2s), WriteSpec + Enqueue, runs the worker loop in a goroutine, waits for the queued stage, sleeps ~200ms, publishes a `start` ControlMessage on `ctrl:<jobID>`, invokes the interaction callback, waits for the terminal `result` event, cancels the loop, and returns the recorded `wire.ResultEvent` plus the docker client + jobID so the caller can assert no leak.

Implement these test funcs (each name MUST contain "Abuse" so `-run Abuse` selects it), each using SMALL limits for speed:
- `TestAbuseForkBomb` (TEST-01): Run = python that spawns processes unboundedly via os.fork() in a loop wrapped in try/except (so it does not crash the test interpreter). Limits Pids=24, WallTimeMs=8000, IdleMs=4000, CpuMs=5000, MemoryMb=128, OutputKb=64. Assert: a terminal result arrives (the sandbox was killed/limited — exitCode non-zero OR Signal set OR TimedOut), assertNoContainerLeak passes, AND the worker SURVIVES — after the fork-bomb job terminates, enqueue a second trivial job (python -c print('alive')) on the SAME running worker stack and assert its stdout contains "alive" and result exitCode 0. (Worker-survival is the load-bearing TEST-01 assertion.)
- `TestAbuseOOM` (TEST-02): Run = python that allocates a list/bytearray far past MemoryMb (e.g. repeatedly append 10MB bytes), MemoryMb=64, WallTimeMs=8000, IdleMs=6000, CpuMs=6000, Pids=64, OutputKb=64. Assert: terminal result shows the kill (exitCode non-zero OR Signal == SIGKILL OR TimedOut false-but-killed — accept exitCode != 0 || Signal != nil), no leak, and worker survives (second trivial job runs).
- `TestAbuseInfiniteLoop` (TEST-03): Run = python `while True: pass` (no stdin, pure CPU). Set WallTimeMs SMALLER than CpuMs so the WALL clock fires first (e.g. WallTimeMs=2000, CpuMs=15000, IdleMs=10000). Assert: `result.TimedOut == true`, no leak.
- `TestAbuseCpuClockEvasion` (bonus, reinforces Pitfall 1): Run = python that loops reading one byte of stdin then spins CPU, structured so wall is generous but CPU is tight (WallTimeMs=15000, CpuMs=2000, IdleMs=10000); interaction callback periodically publishes a single stdin byte so the idle clock never fires. Assert: `result.TimedOut == true` (killed by the CPU clock, surfaced as TimedOut), no leak. If the CPU clock proves timing-flaky on this host, keep the assertion as TimedOut==true (wall is the backstop) and document the observed clock in the SUMMARY — do NOT weaken it to "any termination".

Use no fenced code in the file's comments beyond what compiles; keep each hostile program as an inline `python -c` argv string. Do NOT read sb.Stdout/Stderr directly anywhere — assert only on the recorded published events.
  </action>
  <verify>
    <automated>go vet -tags=abuse ./internal/worker/... && go test -tags=abuse -timeout 600s ./internal/worker/... -run 'TestAbuseForkBomb|TestAbuseOOM|TestAbuseInfiniteLoop|TestAbuseCpuClockEvasion' -v</automated>
  </verify>
  <acceptance_criteria>
- `go test -tags=abuse ... -run TestAbuseForkBomb -v` passes: a terminal result event is recorded, assertNoContainerLeak reports zero containers for the jobID, and the follow-up trivial job prints "alive" with exitCode 0.
- `TestAbuseOOM` passes: terminal result has exitCode != 0 or Signal != nil; no leak; worker survives.
- `TestAbuseInfiniteLoop` passes with the recorded `result.TimedOut == true`; no leak.
- `TestAbuseCpuClockEvasion` passes with `result.TimedOut == true`; no leak.
- The file begins with `//go:build abuse`; `go build ./...` and `go test ./...` (no tags) do NOT compile or run these tests (plain suite stays green).
  </acceptance_criteria>
  <done>The four containment/compute abuse cases run green against real Docker cgroup v2 on this machine, each asserting the correct ResultEvent flag and no container leak, with fork-bomb/OOM also proving worker survival.</done>
</task>

<task type="auto">
  <name>Task 2: stdin/output abuse cases (idle-blocked, EOF clean-exit, giant-output truncation)</name>
  <files>internal/worker/abuse_test.go</files>
  <read_first>
internal/worker/worker.go (stdin routing, stdin_close -> EOF exactly once, output pump), internal/session/interactive.go (idle clock + output budget semantics), packages/contract/gen/go/wire/wire.gen.go (ControlTypeStdinClose, OutputChunkEvent, ResultEvent.Truncated / IdleTimedOut), the `driveAbuseJob` helper added in Task 1.
  </read_first>
  <action>
Append three more test funcs to `internal/worker/abuse_test.go`, reusing the `driveAbuseJob` helper and its interaction callback:
- `TestAbuseIdleBlockedStdin` (TEST-04): Run = python that blocks on `sys.stdin.readline()` / `input()` and never receives input. Limits IdleMs=2000 (small), WallTimeMs=15000 (much larger so idle fires first), CpuMs=15000. Interaction callback sends NO stdin. Assert: `result.IdleTimedOut == true` and `result.TimedOut == false`, no leak.
- `TestAbuseEofCleanExit` (TEST-05): Run = python that reads ALL of stdin to EOF (e.g. `data=sys.stdin.read(); print('got', len(data))`). IdleMs=8000 (generous so it does NOT idle-time-out before close), WallTimeMs=15000. Interaction callback publishes one stdin chunk (e.g. "hello\n") then publishes a `stdin_close` ControlMessage on `ctrl:<jobID>` to deliver EOF. Assert: terminal `result.ExitCode != nil && *ExitCode == 0`, `result.IdleTimedOut == false`, `result.TimedOut == false`, a stdout event contains "got", and no leak. (This proves EOF delivery produces a CLEAN exit, not an idle timeout.)
- `TestAbuseGiantOutput` (TEST-06): Run = python that floods stdout far past OutputKb (e.g. loop printing a long line tens of thousands of times, or `print('X'*1024)` in a large loop). OutputKb=32 (small), WallTimeMs=20000, IdleMs=15000, CpuMs=20000, MemoryMb=128. No stdin. Assert: `result.Truncated == true`, the job still reaches a terminal result (the worker kept draining — no deadlock), no leak, AND assert the suite did not hang (rely on the per-test timeout; additionally assert total recorded stdout bytes are bounded — sum of OutputChunkEvent.Chunk lengths is on the order of the cap, not the multi-MB the program tried to emit, confirming truncation rather than unbounded buffering).
Keep limits small so the whole file runs in seconds-to-low-minutes. Assert only on recorded published events.
  </action>
  <verify>
    <automated>go test -tags=abuse -timeout 600s ./internal/worker/... -run 'TestAbuseIdleBlockedStdin|TestAbuseEofCleanExit|TestAbuseGiantOutput' -v</automated>
  </verify>
  <acceptance_criteria>
- `TestAbuseIdleBlockedStdin` passes with recorded `result.IdleTimedOut == true` and `result.TimedOut == false`; no leak.
- `TestAbuseEofCleanExit` passes with recorded `result.ExitCode` dereferencing to 0, `IdleTimedOut == false`, `TimedOut == false`, a stdout chunk containing "got"; no leak.
- `TestAbuseGiantOutput` passes with recorded `result.Truncated == true`, a terminal result reached within the test timeout (no deadlock), and total recorded stdout bytes bounded near the OutputKb cap (not the full flood); no leak.
- Running the FULL suite `go test -tags=abuse ... -run Abuse -v` shows all 7 Abuse tests PASS.
  </acceptance_criteria>
  <done>The idle, EOF-clean-exit, and giant-output cases run green, each asserting the correct ResultEvent flag (IdleTimedOut / clean ExitCode 0 / Truncated) and no leak; the complete `-run Abuse` selection passes end to end.</done>
</task>

<task type="auto">
  <name>Task 3: Align the make abuse target + run the full suite to green and capture real output</name>
  <files>Makefile</files>
  <read_first>
Makefile (the existing `abuse` target points at `./apps/worker/...`; the suite lives in `./internal/worker/...`), internal/worker/abuse_test.go (the suite + its `-run Abuse` selectable names, from Tasks 1-2).
  </read_first>
  <action>
Edit ONLY the `abuse` target recipe in the Makefile so its path matches where the suite actually lives: change `./apps/worker/...` to `./internal/worker/...`, keeping `-tags=abuse -timeout 600s ... -run Abuse -v`. Update the target's `##` help text to note it requires Docker (cgroup v2) + a redis:7 reachable at TEST_REDIS_URL or localhost:6381 + the executor/python:3.12 image, mirroring the `test-worker` target's prerequisites. Do not change any other target.

Then ACTUALLY RUN the full suite on this machine and confirm every case passes. Ensure a redis:7 is reachable on the port the harness expects (TEST_REDIS_URL or localhost:6381 — start one with `docker run -d -p 6381:6379 redis:7` if needed) and that `executor/python:3.12` is present (it is). Run `make abuse` and capture the real output. Paste the actual `--- PASS` lines for all 7 Abuse tests and the final `ok ... internal/worker` line into the plan SUMMARY. If any case fails or is flaky, fix the test (limits/timing) until it is reliably green — these are the most important tests in the project; do not leave them skipped or red.
  </action>
  <verify>
    <automated>grep -v '^#' Makefile | grep -A2 '^abuse:' | grep -q 'internal/worker' && make abuse</automated>
  </verify>
  <acceptance_criteria>
- The `abuse` Makefile target invokes `go test -tags=abuse ... -run Abuse -v ./internal/worker/...` (verified by the grep gate matching `internal/worker` in the recipe).
- `make abuse` exits 0 with `--- PASS` for all 7 Abuse tests (ForkBomb, OOM, InfiniteLoop, CpuClockEvasion, IdleBlockedStdin, EofCleanExit, GiantOutput) and a final `ok github.com/teovillanueva/code-runner/internal/worker` line.
- The SUMMARY contains the pasted real run output (the PASS lines and the per-case observed result flags: timedOut=true for InfiniteLoop, idleTimedOut=true for IdleBlockedStdin, exitCode=0 for EofCleanExit, truncated=true for GiantOutput).
- `go test ./...` (no tags) still passes and does NOT run the abuse suite.
  </acceptance_criteria>
  <done>`make abuse` is aligned to the real suite path and passes green on this machine with the run output captured in the SUMMARY, proving real cgroup v2 containment for all six abuse cases plus the CPU-evasion bonus.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted code -> sandbox | The abuse programs ARE the untrusted input; the suite verifies the sandbox boundary holds (pids/memory/clocks/output caps) and the trusted worker survives. |
| worker -> Docker socket | The worker (trusted) drives the host socket; the suite asserts no sandbox container leaks past teardown. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Denial of Service | worker process under fork bomb / OOM | mitigate | TestAbuseForkBomb / TestAbuseOOM assert the sandbox is killed AND the worker survives a follow-up job |
| T-04-02 | Denial of Service | worker memory under output flood | mitigate | TestAbuseGiantOutput asserts Truncated=true, bounded recorded bytes, no deadlock (worker keeps draining) |
| T-04-03 | Elevation/Resource | CPU smuggled behind interactive stdin | mitigate | TestAbuseCpuClockEvasion asserts the CPU clock (TimedOut) kills a read-a-byte-then-spin program |
| T-04-04 | Resource leak | orphaned containers after kill | mitigate | assertNoContainerLeak (label code-runner.jobId) after every case |
| T-04-SC | Tampering | go test dependency installs | accept | No new third-party packages are added; the suite reuses existing deps (testify, go-redis, moby SDK). No package-legitimacy gate needed. |
</threat_model>

<verification>
- `go test ./...` (no tags) green — abuse suite excluded by build tag.
- `make abuse` green — all 7 Abuse tests pass against real Docker cgroup v2 + redis on this machine.
- Each abuse case asserts the correct published ResultEvent flag and zero container leak.
- Fork-bomb and OOM additionally assert worker survival via a follow-up trivial job.
</verification>

<success_criteria>
- `internal/worker/abuse_test.go` exists, builds only under `-tags=abuse`, and contains all 6 abuse cases (TEST-01..06) plus the CPU-evasion bonus, driven through the full worker path.
- `make abuse` is aligned to `./internal/worker/...` and passes green with real output captured.
- Plain `go test ./...` remains green and does not run the suite.
</success_criteria>

<output>
Create `.planning/phases/04-abuse-suite-safety-validation/04-01-SUMMARY.md` when done, including the pasted real `make abuse` PASS output.
</output>
