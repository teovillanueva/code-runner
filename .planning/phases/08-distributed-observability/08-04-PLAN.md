---
phase: 08-distributed-observability
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - internal/runner/docker.go
  - internal/session/interactive.go
  - internal/session/pump.go
  - internal/jobstore/queue.go
  - internal/jobstore/capacity.go
  - internal/reaper/reaper.go
  - internal/publisher/publisher.go
  - internal/runner/metrics_test.go
  - internal/publisher/publisher_test.go
autonomous: true
requirements: [OBS-06, OBS-07]

must_haves:
  decisions_implemented: [D-05, D-06]
  truths:
    - "Decisions implemented: D-05 (instrument-type mapping — Int64ObservableGauge for queue depth + slots used/max, Float64Histogram for sandbox create/kill + publish latency, Int64Counter for reaper orphans / publish errors / output bytes), D-06 (code_runner.* dotted namespace + OTel semconv duration units, e.g. code_runner.sandbox.create.duration / code_runner.queue.depth)"
    - "Queue depth and sandbox slots used/max are emitted as observable gauges (callback reads LLEN / in-memory semaphore)"
    - "sandbox.create and sandbox.kill latency are recorded as HISTOGRAMS (code_runner.sandbox.create.duration / .kill.duration) — distinct from the sandbox.create SPAN, which is owned by 08-02"
    - "Interactive output bytes are recorded as a counter (never per-chunk spans)"
    - "Reaper orphans, soketi publish latency + errors are emitted"
  artifacts:
    - path: "internal/publisher/publisher.go"
      provides: "soketi publish latency histogram + error counter at the Trigger chokepoint"
      contains: "code_runner.publish"
    - path: "internal/jobstore/queue.go"
      provides: "QueueDepth(ctx) LLEN wrapper feeding the async queue-depth gauge"
      contains: "QueueDepth"
    - path: "internal/runner/docker.go"
      provides: "sandbox.create/.kill latency histogram .Record (inside the create/kill funcs; the create SPAN context is threaded in by 08-02)"
      contains: "code_runner.sandbox"
  key_links:
    - from: "internal/jobstore/queue.go"
      to: "code_runner.queue.depth observable gauge"
      via: "QueueDepth(ctx) LLEN jobs:queue in the gauge callback"
      pattern: "LLen|QueueDepth"
    - from: "internal/runner/docker.go"
      to: "code_runner.sandbox.create.duration / .kill.duration"
      via: "Float64Histogram.Record inside Create/Kill"
      pattern: "sandbox.(create|kill).duration"
    - from: "internal/publisher/publisher.go"
      to: "code_runner.publish.duration / code_runner.publish.errors"
      via: "wrap pusherTriggerer.Trigger"
      pattern: "publish.(duration|errors)"
---

<objective>
Worker-side breadth slice: emit the full Go-side OBS-06 domain-metric set now that the worker meter is initialized (08-02). Wire observable gauges (queue depth, slots used/max), latency histograms (sandbox create/kill, soketi publish), the output-bytes counter, and counters (reaper orphans, soketi publish errors) at the exact PATTERNS-mapped sites.

Purpose: After the connected-trace slice (02+03) proves the hardest risk, this delivers operational breadth on the worker — every Go-side metric an operator needs, exported via OTLP push (the single export path; OBS-05 pull is dropped). Metric attributes stay low-cardinality (`terminal_state`, `language`, `reason`); `job_id` never goes on metrics (RESEARCH anti-pattern). All metric sources reuse existing signals (LLEN, in-memory semaphore, ResultEvent flags, enqueuedAtMs) — no new bookkeeping.

File-ownership note (the sandbox.create split): 08-02 owns the `sandbox.create` SPAN and the context threading through `internal/runner/docker.go`'s create path. THIS plan owns the create/kill latency HISTOGRAM `.Record` calls inside the same `Create`/`Kill` functions. Span (08-02) and histogram (08-04) are separate concerns in the same file — do NOT re-thread context or re-touch the span lines; add only the histogram instrument + `.Record`.

The API-side metrics (admission/ratelimit rejection counters) and the remaining `console.*`→pino migration moved to 08-04b (disjoint TS files, same wave, runs in parallel).

Output: instrumented `internal/runner`, `internal/session`, `internal/jobstore`, `internal/reaper`, `internal/publisher`; manual-reader metric assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-distributed-observability/08-SPEC.md
@.planning/phases/08-distributed-observability/08-CONTEXT.md
@.planning/phases/08-distributed-observability/08-RESEARCH.md
@.planning/phases/08-distributed-observability/08-PATTERNS.md
@.planning/phases/08-distributed-observability/08-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Worker domain metrics — gauges, latency histograms, output-bytes counter, reaper + publish metrics</name>
  <files>internal/runner/docker.go, internal/session/interactive.go, internal/session/pump.go, internal/jobstore/queue.go, internal/jobstore/capacity.go, internal/reaper/reaper.go, internal/publisher/publisher.go, internal/runner/metrics_test.go, internal/publisher/publisher_test.go</files>
  <read_first>
    - 08-RESEARCH.md §"Pattern 5: Domain metrics — Go instruments" (lines 346-365) — `otel.Meter("code-runner-worker")`, `Int64ObservableGauge` + `RegisterCallback` for queue depth/slots, `Float64Histogram` (unit `s`) for create latency, counters; D-05/D-06/D-07 instrument mapping.
    - 08-RESEARCH.md §"Pitfall 5: Reading Redis inside an async-gauge callback" (lines 417-421) — short-timeout ctx, skip observation on error, slots from in-memory semaphore (no Redis).
    - 08-RESEARCH.md §"Anti-Patterns to Avoid" (367-372) — NO per-chunk spans; NO high-cardinality attributes (no job_id on metrics); never open an admin port.
    - 08-PATTERNS.md per-file sections: `internal/runner/docker.go` (lines 230-235, Create @158/`ContainerCreate` @292 + Kill @505 → create/kill latency histograms); `internal/session/interactive.go` + `pump.go` (lines 239-244, output-byte counter at pump.go `Run()` ~line 58 budget path — NOT per chunk); `internal/jobstore/queue.go`+`capacity.go` (lines 248-252, add `QueueDepth(ctx)` LLEN wrapper, slots gauge from in-memory semaphore); `internal/reaper/reaper.go` (lines 256-260, orphans counter after `ContainerRemove` ~line 190-197); `internal/publisher/publisher.go` (lines 264-268, wrap `pusherTriggerer.Trigger` lines 37-39 → `code_runner.publish.duration` histogram + `code_runner.publish.errors` counter).
    - 08-02-PLAN.md Task 2 — the `sandbox.create` SPAN + the `ctx` threaded into `docker.go`'s create path are wired by 08-02. THIS plan adds ONLY the create/kill latency histogram `.Record` inside `Create`/`Kill`; do NOT alter the span/context-threading lines (clean file ownership: span lines = 08-02, histogram lines = 08-04).
    - 08-RESEARCH.md §"Validation Architecture" Test Tooling Notes (lines 582-586) — Go metrics via `sdk/metric.NewManualReader()` + `reader.Collect(ctx,&rm)`.
    - The actual source files: internal/runner/docker.go (Create/Kill), internal/session/pump.go (Run/budget), internal/jobstore/queue.go (Claim/Enqueue method shape), internal/jobstore/capacity.go (free-slot accounting), internal/reaper/reaper.go (reapContainer), internal/publisher/publisher.go (triggerer interface + pusherTriggerer adapter).
  </read_first>
  <behavior>
    - `code_runner.queue.depth` (Int64ObservableGauge, unit `{job}`) observes `LLEN jobs:queue` via a new `QueueDepth(ctx)`; on Redis error the callback skips the observation (no stale/zero forced).
    - `code_runner.slots.used` (Int64ObservableGauge, unit `{slot}`) observes used slots from the in-memory semaphore (no Redis call).
    - `code_runner.sandbox.create.duration` and `code_runner.sandbox.kill.duration` (Float64Histogram, unit `s`) record around Create/Kill — these are HISTOGRAMS, distinct from the `sandbox.create` SPAN wired in 08-02.
    - `code_runner.output.bytes` (Int64Counter) increments by forwarded byte count in the pump budget path; an interactive job with N chunks produces a nonzero output-bytes value and ZERO per-chunk spans.
    - `code_runner.reaper.orphans` (Int64Counter) increments once per successfully removed orphan container.
    - `code_runner.publish.duration` (histogram, unit `s`) records each soketi Trigger; `code_runner.publish.errors` (counter) increments on a non-nil Trigger error.
    - Metric attributes are low-cardinality only (`language`, `reason`); no `job_id` on any metric.
  </behavior>
  <action>
    Add `otel.Meter("code-runner-worker")`-derived instruments at the PATTERNS-mapped sites. Add `QueueDepth(ctx context.Context) (int64, error)` to `internal/jobstore/queue.go` wrapping `s.client.LLen(ctx, keys.JobQueue)` (mirror the existing `Claim`/`Enqueue` method shape); register the queue-depth + slots-used observable gauges in the worker wiring (the `RegisterCallback` reads `QueueDepth` with a short-timeout ctx per Pitfall 5 and reads slots from the in-memory semaphore — register where the meter + semaphore are both in scope, e.g. worker setup; do NOT modify worker.go's run-loop trace code from 08-02, only add the gauge registration alongside the meter). In `internal/runner/docker.go` add a `Float64Histogram` for create and one for kill and `.Record` the elapsed seconds inside the existing `Create`/`Kill` bodies — add ONLY the histogram instrument + `.Record`; do NOT touch the `sandbox.create` span or the `ctx` threading that 08-02 added to the same function (clean ownership). In `internal/session/pump.go` increment the `code_runner.output.bytes` Int64Counter by forwarded bytes in the budget path — EXPLICITLY no per-chunk span. In `internal/reaper/reaper.go` increment `code_runner.reaper.orphans` after a successful `ContainerRemove`. In `internal/publisher/publisher.go` wrap the `pusherTriggerer.Trigger` adapter to time the call (`code_runner.publish.duration`, unit `s`) and increment `code_runner.publish.errors` on error. Write `internal/runner/metrics_test.go` and `internal/publisher/publisher_test.go` using `sdkmetric.NewManualReader()` + `reader.Collect` to assert instrument names/values/attributes deterministically. Never attach `job_id`, user code, stdin, or secrets to any metric attribute (RESEARCH anti-pattern + Security V8). Do NOT open any HTTP/admin port (OBS-05 dropped).
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/runner/ ./internal/session/ ./internal/jobstore/ ./internal/reaper/ ./internal/publisher/</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/publisher/` asserts via a ManualReader that `code_runner.publish.duration` records and `code_runner.publish.errors` increments on a forced Trigger error.
    - `go test ./internal/runner/` asserts `code_runner.sandbox.create.duration`/`code_runner.sandbox.kill.duration` HISTOGRAMS are recorded (distinct from the 08-02 span).
    - A test asserts the queue-depth gauge callback observes the LLEN value and skips on Redis error; slots gauge reads the in-memory semaphore.
    - A test asserts N forwarded chunks → nonzero `code_runner.output.bytes` and zero per-chunk spans.
    - `grep -rn 'job_id\|jobID\|jobId' internal/ | grep -i 'WithAttributes\|attribute.String'` shows NO job_id on metric attributes.
    - `grep -rn "http.ListenAndServe\|http.Server" apps/worker internal/ | grep -v '_test.go'` adds no new listener.
  </acceptance_criteria>
  <done>All worker-side OBS-06 metrics (gauges, latency histograms, output-bytes counter, reaper orphans, publish latency/errors) emit with low-cardinality attributes; no per-chunk spans; no admin port; the create/kill histograms coexist cleanly with the 08-02 sandbox.create span in docker.go.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker → OTLP Collector | Metric egress (push only). No inbound surface; OBS-05 admin port dropped. |
| Redis → async-gauge callback | The queue-depth gauge reads LLEN on the export interval (untrusted timing source). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Denial of Service (backend) | high-cardinality metric attributes | mitigate | All counters/histograms use low-cardinality attributes (`terminal_state`, `language`, `reason`); Task asserts no `job_id`/user-string on metric attributes. |
| T-08-11 | Denial of Service | async-gauge callback reads Redis | mitigate | Short-timeout ctx in the gauge callback; skip observation on error (Pitfall 5); slots read from in-memory semaphore (no Redis). |
| T-08-13 | Elevation / Tampering | no new listener | accept | No admin/metrics HTTP port opened (OBS-05 dropped); metric export is outbound OTLP push only; worker trust boundary unchanged. |
</threat_model>

<verification>
- `go build ./...` + `go test ./internal/runner/ ./internal/session/ ./internal/jobstore/ ./internal/reaper/ ./internal/publisher/` pass.
- ManualReader assertions confirm the listed instruments + low-cardinality attributes.
- No new HTTP listener in the worker; create/kill histograms coexist with the 08-02 sandbox.create span in docker.go (no line conflict).
</verification>

<success_criteria>
- The full worker-side OBS-06 metric set is emitted via OTLP push (the single export path; OBS-05 pull dropped).
- Metric attributes are low-cardinality; `job_id` is never on metrics.
- Create/kill latency histograms (08-04) are cleanly separated from the sandbox.create span (08-02) in docker.go.
</success_criteria>

<output>
Create `.planning/phases/08-distributed-observability/08-04-SUMMARY.md` when done.
</output>
