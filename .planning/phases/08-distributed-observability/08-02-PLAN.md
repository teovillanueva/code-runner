---
phase: 08-distributed-observability
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - internal/otelinit/otelinit.go
  - internal/otelinit/otelinit_test.go
  - internal/logging/handler.go
  - internal/logging/handler_test.go
  - apps/worker/main.go
  - internal/worker/worker.go
  - internal/runner/docker.go
  - go.mod
  - go.sum
autonomous: true
requirements: [OBS-01, OBS-02, OBS-03, OBS-04, OBS-07]

must_haves:
  decisions_implemented: [D-03, D-04, D-06, D-07, D-13]
  truths:
    - "Decisions implemented: D-03 (worker side: stdout JSON always via custom slog handler + OTLP logs when configured; job_id via worker custom slog handler ctx key), D-04 (worker stays HTTP-server-free / no admin port — OBS-05 dropped, asserted by the no-listener grep gate), D-06 (code_runner.* namespace + semconv units on the worker terminal counter + queue-time histogram), D-07 (one code_runner.jobs.terminal counter with low-cardinality terminal_state attribute), D-13 (worker phase spans LINK to the API execute span, not parent-child)"
    - "With OTEL_EXPORTER_OTLP_ENDPOINT unset, the worker installs no exporters, opens no new port, and behaves exactly as today"
    - "On claim the worker extracts the JobSpec traceparent and starts a root span LINKED (not parented) to the API execute span, sharing the API trace_id"
    - "The worker emits named phase spans claim (root), sandbox.create (a real span around the internal/runner create call, NOT a placeholder), handshake.wait, compile, run, publish.result mapped to the run loop"
    - "An interactive job emitting N stdout chunks produces zero per-chunk spans"
    - "An idle-killed job increments code_runner.jobs.terminal{terminal_state=idle_timed_out}"
    - "Worker stdout log lines are valid JSON carrying trace_id/span_id/job_id within a span context"
  artifacts:
    - path: "internal/otelinit/otelinit.go"
      provides: "Env-gated OTel provider construction (traces+metrics+logs) via autoexport; W3C propagator; combined shutdown"
      contains: "IsNone"
    - path: "internal/logging/handler.go"
      provides: "Custom slog.Handler wrapping slog.JSONHandler that injects trace_id/span_id/job_id from ctx"
      contains: "SpanContextFromContext"
    - path: "internal/worker/worker.go"
      provides: "traceparent extract + linked root span + phase spans (incl. real sandbox.create span around the runner create call) + terminal-state counter + time-in-queue"
      contains: "WithLinks"
  key_links:
    - from: "internal/worker/worker.go"
      to: "otel.Tracer(\"code-runner-worker\")"
      via: "trace.WithLinks(trace.Link{SpanContext: linkedSC})"
      pattern: "WithLinks"
    - from: "internal/worker/worker.go"
      to: "spec.Traceparent"
      via: "propagation.TraceContext{}.Extract"
      pattern: "propagation\\.(TraceContext|MapCarrier)"
    - from: "apps/worker/main.go"
      to: "internal/otelinit"
      via: "otelinit.Init(ctx) + defer shutdown"
      pattern: "otelinit\\.Init"
---

<objective>
Build the Go side of the one connected trace (MVP slice): initialize the worker OTel SDK env-gated as a true no-op when unset, install the custom stdout slog handler for trace correlation, extract the API-injected `traceparent` from JobSpec on claim, and start a LINKED root span plus the worker phase spans. After this plan, a worker-side execution produces named phase spans under the API's `trace_id` and trace-correlated stdout JSON — turning the 08-01 Go round-trip test GREEN.

Purpose: This is half of the headline acceptance criterion (one execution → one trace shared between API and worker). The worker links rather than parents because `/v1/execute` returns 202 before the run starts (D-13). The no-op gate (OBS-01) is the make-or-break correctness property: neither the Go `autoexport` factory nor the SDK is a no-op by default.

Output: `internal/otelinit` (provider bootstrap), `internal/logging` (stdout JSON correlation handler), worker run-loop instrumentation (extract+link, phase spans, terminal-state counter, time-in-queue), and the OTel Go modules promoted to direct deps at the RESEARCH-pinned versions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-distributed-observability/08-SPEC.md
@.planning/phases/08-distributed-observability/08-CONTEXT.md
@.planning/phases/08-distributed-observability/08-RESEARCH.md
@.planning/phases/08-distributed-observability/08-PATTERNS.md
@.planning/phases/08-distributed-observability/08-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Env-gated OTel provider init (internal/otelinit) + custom stdout slog handler (internal/logging)</name>
  <files>internal/otelinit/otelinit.go, internal/otelinit/otelinit_test.go, internal/logging/handler.go, internal/logging/handler_test.go, go.mod, go.sum</files>
  <read_first>
    - 08-RESEARCH.md §"Pattern 2: No-op-when-unset gate (Go via autoexport)" (lines 258-289) — `autoexport.NewSpanExporter/NewMetricReader/NewLogExporter`, `IsNone*` early-return, `otel.SetTextMapPropagator(propagation.TraceContext{})`, combined `errors.Join` shutdown, `global.SetLoggerProvider`.
    - 08-RESEARCH.md §"Pattern 4: Worker stdout trace correlation (custom slog handler)" (lines 324-344) — `ctxHandler struct{ slog.Handler }`, `trace.SpanContextFromContext(ctx)`, `job_id` from a ctx key.
    - 08-RESEARCH.md §"Pitfall 2" (399-403, SDK not no-op by default), §"Pitfall 3" (405-409, Go propagator default is no-op — MUST set TraceContext), §"Pitfall 4" (411-415, otelslog does NOT write stdout).
    - 08-RESEARCH.md §"Standard Stack — Core OTel Go" (lines 49-69) and "Installation" `go get` block (lines 107-119) — exact module versions: otel/sdk v1.44.0, sdk/metric v1.44.0, otel/log + sdk/log v0.20.0, otlpmetrichttp v1.44.0, otlploghttp v0.20.0, contrib/exporters/autoexport v0.69.0, contrib/bridges/otelslog v0.19.0.
    - 08-PATTERNS.md §"internal/otelinit/otelinit.go (new)" (lines 166-172) — constructor SHAPE from `internal/publisher/publisher.go:71` `New(cfg)`.
    - 08-PATTERNS.md §"internal/logging/handler.go (new)" (lines 176-180) — no in-repo analog; use RESEARCH Pattern 4 verbatim.
    - internal/publisher/publisher.go (lines 28-39, 71-89) — interface-behind-impl + `New(cfg)` constructor shape to mirror for testability.
    - 08-RESEARCH.md §"Validation Architecture" Wave 0 Gaps (lines 588-597) — `otelinit_test.go` (no-op + provider construction) and `handler_test.go` (stdout JSON correlation) are listed gaps.
  </read_first>
  <behavior>
    - otelinit: with both `OTEL_EXPORTER_OTLP_ENDPOINT` and `OTEL_TRACES_EXPORTER` unset, `Init(ctx)` installs no exporters, sets no global TracerProvider/MeterProvider, and returns a non-nil no-op shutdown func with nil error.
    - otelinit: when an endpoint is configured (test sets `OTEL_TRACES_EXPORTER=none` is the none case; the constructed-provider case is asserted via injecting a test reader/recorder OR by asserting the global propagator is set to W3C TraceContext after Init when endpoint present), `Init` sets `otel.GetTextMapPropagator()` to a W3C TraceContext propagator.
    - logging handler: a record logged WITH a context carrying a valid SpanContext produces stdout JSON containing `trace_id` and `span_id` equal to that span context; logged WITHOUT a span context omits those keys (no empty/zero values forced); a `job_id` in the ctx key appears as `job_id` in the JSON.
    - logging handler: output is always valid JSON (wraps `slog.NewJSONHandler`) regardless of OTEL state (D-03 stdout-always).
  </behavior>
  <action>
    Promote the OTel Go core modules to direct dependencies and add the signal-specific modules via `go get` at the exact RESEARCH-pinned versions (otel@v1.44.0, otel/sdk@v1.44.0, otel/sdk/metric@v1.44.0, otel/log@v0.20.0, otel/sdk/log@v0.20.0, otlpmetric/otlpmetrichttp@v1.44.0, otlplog/otlploghttp@v0.20.0, contrib/exporters/autoexport@v0.69.0, contrib/bridges/otelslog@v0.19.0); otlptracehttp + otel/trace + otel/metric are already indirect — promote them. Run `go mod tidy`. Create `internal/otelinit/otelinit.go` with `Init(ctx context.Context) (shutdown func(context.Context) error, err error)` following RESEARCH Pattern 2: early-return a no-op shutdown when `OTEL_EXPORTER_OTLP_ENDPOINT` and `OTEL_TRACES_EXPORTER` are both empty (use `autoexport.IsNone*` helpers where applicable); otherwise build TracerProvider (WithBatcher), MeterProvider (WithReader from `autoexport.NewMetricReader`), LoggerProvider (BatchProcessor from `autoexport.NewLogExporter`), set the globals, and ALWAYS call `otel.SetTextMapPropagator(propagation.TraceContext{})` (W3C — REQUIRED for cross-lang, RESEARCH Pitfall 3); return a combined `errors.Join` shutdown. Use `OTEL_SERVICE_NAME`/`OTEL_RESOURCE_ATTRIBUTES` via `resource.WithFromEnv()`. Create `internal/logging/handler.go` with an exported constructor (e.g. `NewCtxHandler(base slog.Handler) slog.Handler`) wrapping a `slog.JSONHandler`, pulling `trace_id`/`span_id` from `trace.SpanContextFromContext(ctx)` (only when `IsValid()`) and `job_id` from an exported ctx-key helper. Write the two test files using stdlib `testing`; for otelinit use `t.Setenv` to toggle env and a `sdk/trace/tracetest` recorder / `sdk/metric.NewManualReader` injection point where needed; for the handler, capture output to a `bytes.Buffer` and assert JSON keys. Do NOT log or attribute any secret (EXECUTOR_API_TOKEN, SOKETI_APP_SECRET) or user code/stdin (RESEARCH Security V7/V8).
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/otelinit/ ./internal/logging/</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` succeeds with the new direct deps at the pinned versions; `go.mod` lists otel/sdk v1.44.0, otel/log v0.20.0, contrib/exporters/autoexport v0.69.0, contrib/bridges/otelslog v0.19.0.
    - `go test ./internal/otelinit/` asserts: with endpoint+exporter env unset, `Init` returns a non-nil no-op shutdown and installs no global TracerProvider beyond the default no-op; with endpoint set, the global propagator is W3C TraceContext.
    - `go test ./internal/logging/` asserts a record logged within a valid span context yields JSON containing matching `trace_id`/`span_id`, and a `job_id` ctx value appears as `job_id`; output is valid JSON in both OTEL states.
    - No secret or user code/stdin string appears in handler output or test fixtures.
  </acceptance_criteria>
  <done>Env-gated provider bootstrap + stdout JSON correlation handler exist and are unit-tested; no-op-when-unset proven; W3C propagator always set.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire OTel at worker boot + extract traceparent, linked root span, phase spans, terminal counter</name>
  <files>apps/worker/main.go, internal/worker/worker.go, internal/runner/docker.go, internal/worker/trace_test.go</files>
  <read_first>
    - 08-RESEARCH.md §"Pattern 3 (Extract + link)" (lines 308-322) — `propagation.MapCarrier`, `propagation.TraceContext{}.Extract`, `trace.SpanContextFromContext`, `otel.Tracer("code-runner-worker").Start(ctx, "claim", trace.WithLinks(trace.Link{SpanContext: linkedSC}))`.
    - 08-RESEARCH.md §"Pattern 5: Domain metrics — Go instruments" (lines 346-365) — `code_runner.jobs.terminal` Int64Counter with low-cardinality `terminal_state` attribute (D-07).
    - 08-PATTERNS.md §"apps/worker/main.go (modified)" (lines 184-194) — insertion site: top of `run(ctx)` call `otelinit.Init`, `defer shutdown`, then `slog.SetDefault(slog.New(logging.NewCtxHandler(base)))`. Existing slog calls (main.go:50,73-81,95,116,123,159) keep working.
    - 08-PATTERNS.md §"internal/worker/worker.go (modified)" (lines 198-227) — extract+link site at `runJobFromSpec` start (~line 229 `jobID := spec.JobId`); phase-span site table (claim @229, sandbox.create @295, handshake.wait @363-388, compile @397-427, run @517, publish.result @333-336) — `sandbox.create` is a REAL span started in the run loop around the `internal/runner` create call (~@295); the active span context is passed down so `internal/runner/docker.go`'s `ContainerCreate` runs within that span; terminal-state counter at the terminal decision (lines 522-526) + each `teardown(...)`; time-in-queue `now - spec.EnqueuedAtMs` at claim.
    - internal/worker/worker.go — read `runJobFromSpec`, the `parkLoop`, the `spec.Compile != nil` block, `session.RunInteractive` call, the teardown/`w.pub.Result` path, and the terminal-state decision to place spans/counters exactly at those sites.
    - internal/runner/docker.go — the `Create`/`ContainerCreate` call site (~line 158/292). FILE OWNERSHIP: 08-02 owns ONLY the span plumbing — propagate the active context (e.g. accept/thread `ctx`) so `ContainerCreate` executes inside the `sandbox.create` span started by the run loop. 08-04 owns the create/kill latency histogram `.Record` calls inside the same function. The two plans MUST NOT rewrite the same lines: 08-02 adds the span/context threading, 08-04 adds the histogram recording. Touch only the context-threading lines here.
    - internal/worker/trace_test.go (from 08-01) — the round-trip test whose helper this task replaces with the real extract logic; this task makes it GREEN against production code and adds a PhaseSpans recorder test.
    - 08-RESEARCH.md §"Validation Architecture" Test Tooling Notes (lines 582-586) — `tracetest.NewSpanRecorder()` as a SpanProcessor for asserting span names + links + shared TraceID; Wave 0 gap `internal/worker/trace_test.go` (OBS-02, OBS-03).
  </read_first>
  <behavior>
    - On claim, given a JobSpec with a valid `traceparent` (`*string`), the worker root span's links include a SpanContext whose `TraceID()` equals the traceparent's trace_id (span LINK, not parent — assert via `tracetest.SpanRecorder`, `span.Links()`); with `traceparent` nil, the root span starts a fresh trace (no link, no panic).
    - An execution produces spans named `claim` (root), `sandbox.create`, `handshake.wait`, `compile`, `run`, `publish.result`. `sandbox.create` is a REAL span (not a placeholder/reserved name): the run loop starts it around the `internal/runner` create call and threads its context into `docker.go`'s `ContainerCreate`. (The separate create-latency *histogram* `code_runner.sandbox.create.duration` is wired in 08-04 inside the same function — span here, histogram there, no overlap.)
    - An interactive job that forwards N stdout chunks produces ZERO spans named per chunk (OBS-03 anti-pattern) — assert no span name resembling a per-chunk pattern in the recorder.
    - An idle-killed terminal path increments `code_runner.jobs.terminal` with attribute `terminal_state=idle_timed_out`; a normal completion increments with `terminal_state=done`; warmup-timeout path increments with `terminal_state=error` (or a warmup attribute per D-07 low-cardinality set).
  </behavior>
  <action>
    In `apps/worker/main.go` `run(ctx)`, before the existing config/loop setup, call `shutdown, err := otelinit.Init(ctx)` (handle err), `defer shutdown(context.Background())`, then set the default slog handler to `logging.NewCtxHandler(slog.NewJSONHandler(os.Stdout, nil))`. In `internal/worker/worker.go` at `runJobFromSpec` start, build a `propagation.MapCarrier` from `spec.Traceparent`/`spec.Tracestate` (nil-safe), `propagation.TraceContext{}.Extract` it, take `trace.SpanContextFromContext`, and start the root span `claim` via `otel.Tracer("code-runner-worker").Start(ctx, "claim", trace.WithLinks(trace.Link{SpanContext: linkedSC}))` with `defer root.End()`. Start child spans `sandbox.create`, `handshake.wait`, `compile`, `run`, `publish.result` from that ctx at the PATTERNS-mapped sites (NOT per chunk). For `sandbox.create`: start a real span around the `internal/runner` create call (~worker.go @295) and pass the active span context down into `internal/runner/docker.go` so `ContainerCreate` executes within the `sandbox.create` span (thread `ctx` through the create path; touch ONLY the context-threading lines in docker.go — the create/kill latency histogram `.Record` is added by 08-04 in the same function, so do not add metric recording here). Add a package-level meter `otel.Meter("code-runner-worker")` and the `code_runner.jobs.terminal` Int64Counter; increment it at the terminal-state decision and each teardown with a low-cardinality `terminal_state` attribute derived from `wire.JobState`/`result.TimedOut`/`result.IdleTimedOut` (use the exact attribute values `done`/`killed`/`timed_out`/`idle_timed_out`/`cpu_exceeded`/`error` per D-07). Record a `code_runner.queue.time` histogram of `now - spec.EnqueuedAtMs` at claim (unit `s`, convert ms→s). Switch hot-path `slog.Info/Error` calls that have a span ctx to the `*Context` variants so trace fields ride. Replace the 08-01 test helper in `trace_test.go` with the real extract path and add a `PhaseSpans`/`Metrics` recorder test using `tracetest.SpanRecorder` and `sdkmetric.NewManualReader`. Keep all existing run-loop behavior intact; do NOT open any HTTP port (OBS-05 dropped); do NOT put `job_id` on metric attributes (RESEARCH anti-pattern — job_id is on spans/logs only).
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/worker/ ./apps/worker/</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/worker/ -run Traceparent` (from 08-01) passes against the real extract code.
    - A recorder-based test asserts the root `claim` span has a LINK whose TraceID equals the injected traceparent's trace_id (not a parent), and that child spans `sandbox.create`/`handshake.wait`/`compile`/`run`/`publish.result` exist under the same trace. The `sandbox.create` span MUST be present in the in-memory `tracetest.SpanRecorder` output (proves it is a real span, not a reserved name).
    - A test asserts N forwarded stdout chunks produce zero per-chunk spans.
    - A `sdkmetric.NewManualReader` test asserts `code_runner.jobs.terminal{terminal_state=idle_timed_out}` increments on the idle-killed path.
    - `grep -rn "http.ListenAndServe\|http.Server" apps/worker internal/ | grep -v '_test.go'` returns no NEW listener (worker stays HTTP-server-free; OBS-05 dropped).
    - With OTEL env unset, `go test ./apps/worker/` passes and no telemetry is emitted (no-op gate from Task 1 holds).
  </acceptance_criteria>
  <done>Worker extracts the API traceparent, emits a linked root span + named phase spans (no per-chunk spans), increments the terminal-state counter, records time-in-queue, and logs trace-correlated JSON — the Go half of the one connected trace.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Redis → Worker | Worker extracts the untrusted `traceparent` string from JobSpec on claim. |
| Worker → OTLP Collector | New outbound-only egress (push). No inbound surface added. |
| Worker stdout/spans → operator | Logs and span attributes must never carry secrets or user code/stdin. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Tampering / DoS | `traceparent` extract on claim (worker.go) | mitigate | `propagation.TraceContext{}.Extract` is bounded and fails closed to a fresh trace on malformed/nil input; Task 2 asserts nil + malformed never panic. No new HTTP listener. |
| T-08-04 | Information Disclosure | worker logs + span attributes | mitigate | Custom slog handler only injects `trace_id`/`span_id`/`job_id` (non-secret). Task asserts no secret/code/stdin in output; metric attributes are low-cardinality (`terminal_state`) — `job_id` excluded from metrics. |
| T-08-05 | Information Disclosure / Elevation | telemetry egress as exfil channel | mitigate | OTLP push only, off by default (no-op gate, OBS-01); endpoint is operator-configured env, not attacker-controllable. Worker still talks only to Redis + soketi + (new) OTLP collector — no HTTP call to the API; the existing worker no-HTTP-to-API grep gate still holds. |
| T-08-06 | Denial of Service | OTel SDK startup with OTEL unset | mitigate | No-op gate (Task 1): no exporter constructed, no connection to localhost:4318, no new port — zero measurable startup regression. |
</threat_model>

<verification>
- `go build ./...` and `go test ./internal/otelinit/ ./internal/logging/ ./internal/worker/ ./apps/worker/` pass.
- `go test ./internal/worker/ -run Traceparent` (08-01 round-trip) GREEN against production extract.
- Recorder test proves linked root span + named phase spans (including a real `sandbox.create` span) share the injected trace_id; zero per-chunk spans.
- Manual-reader test proves `code_runner.jobs.terminal{terminal_state=idle_timed_out}` increment.
- No new HTTP listener in the worker (OBS-05 dropped).
</verification>

<success_criteria>
- Worker OTel SDK is a true no-op when OTEL_* unset (no exporter, no port, no startup regression).
- Worker extracts the API-injected traceparent and emits a linked root span + phase spans sharing the API trace_id.
- Interactive output is metrics, never per-chunk spans.
- Terminal-state counter + time-in-queue recorded; stdout logs are trace-correlated JSON.
</success_criteria>

<output>
Create `.planning/phases/08-distributed-observability/08-02-SUMMARY.md` when done.
</output>
