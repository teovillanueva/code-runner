---
phase: 09-artifacts-pullable-run-output
plan: 07
type: execute
wave: 4
depends_on: ["09-02"]
files_modified:
  - docker-compose.yml
  - .env.example
  - deploy/fly/worker/fly.toml
  - docs/deploy-fly.md
autonomous: true
requirements: [R14, R16]
must_haves:
  truths:
    - "docker compose up starts a MinIO service on the code-runner network with a healthcheck and a data volume; the worker points AWS_ENDPOINT_URL_S3 at it, and the bucket is auto-created by the worker's S3Store.EnsureLifecycle (plan 02) — NO mc/init-container bucket-create step is added to compose"
    - ".env.example documents every new var: AWS_* + ARTIFACT_S3_* overrides, the three TTLs, the ordering invariant, the ephemeral-handoff note, and the 1-day lifecycle granularity caveat"
    - "docs/deploy-fly.md documents fly storage create (Tigris) + mapping its injected creds to the worker S3Store env; the same S3Store runs unmodified on Tigris"
    - "deploy/fly/worker/fly.toml reflects the S3 config (Tigris creds as secrets, non-secret TTLs in [env]); the API app needs no S3 creds"
  artifacts:
    - path: "docker-compose.yml"
      provides: "minio service + worker S3 env wiring (bucket auto-created by the worker, no init container)"
      contains: "minio"
    - path: ".env.example"
      provides: "Artifacts / object storage env documentation block"
      contains: "AWS_ENDPOINT_URL_S3"
    - path: "deploy/fly/worker/fly.toml"
      provides: "Tigris secrets list + non-secret TTL [env]"
      contains: "ARTIFACT_S3_OBJECT_TTL"
    - path: "docs/deploy-fly.md"
      provides: "fly storage create step + credential-mapping table"
      contains: "fly storage create"
  key_links:
    - from: "docker-compose.yml worker service"
      to: "minio service"
      via: "AWS_ENDPOINT_URL_S3=http://minio:9000 + BUCKET_NAME + creds + depends_on (bucket created by S3Store.EnsureLifecycle)"
      pattern: "AWS_ENDPOINT_URL_S3"
    - from: "deploy/fly/worker/fly.toml"
      to: "Tigris bucket"
      via: "fly storage create injects AWS_* secrets onto the worker app"
      pattern: "BUCKET_NAME"
---

<objective>
Make object storage runnable locally and deployable on Fly: add a MinIO service to the dev compose stack wired to the worker, document every new env var (S3 creds + the three TTLs + the ordering invariant + the ephemeral-handoff + 1-day-granularity caveats) in `.env.example`, and document/wire Tigris provisioning (`fly storage create`) into the worker app's `S3Store` env in `deploy/fly/worker/fly.toml` + `docs/deploy-fly.md` — the API app needs no S3 creds. The dev MinIO bucket is created at runtime by the worker's `S3Store.EnsureLifecycle` (plan 02, which calls BucketExists+MakeBucket-if-absent), so compose adds NO `mc mb` / init-container bucket-create step.

Purpose: R14 (dev infra + docs) and R16 (Fly/Tigris) make the S3Store from plan 02 actually usable — locally against MinIO and in prod against Tigris — with zero code change between them, and with a fresh MinIO/Tigris needing no manual bucket creation.

Output: Updated compose + .env.example + fly worker config + deploy doc. Depends on plan 02 (the env var names the S3Store reads + its self-creating-bucket EnsureLifecycle).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-artifacts-pullable-run-output/09-SPEC.md
@.planning/phases/09-artifacts-pullable-run-output/09-CONTEXT.md
@.planning/phases/09-artifacts-pullable-run-output/09-PATTERNS.md

<interfaces>
<!-- Existing infra shapes to extend. -->

From docker-compose.yml:
  redis service (26-35): image, restart unless-stopped, networks [code-runner], healthcheck.
  worker service (120-160): environment block reading REDIS_URL/SOKETI_*/WORKER_*/DOCKER_HOST etc.; depends_on redis+soketi with condition: service_healthy; networks [code-runner].
  networks: code-runner (bridge).

From .env.example: documented blocks with comments per var (header 1-21, API 22-23, Worker 25-42, Stub 44+).

From deploy/fly/worker/fly.toml:
  [env] block (13-30) with non-secret config (REDIS_URL, SOKETI_HOST/PORT, WORKER_MAX_SANDBOXES, SANDBOX_IMAGE_REGISTRY) + a `# Secrets (fly secrets set ...)` comment list (SOKETI_APP_SECRET, GHCR_TOKEN).

The S3Store (plan 02) reads: AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY / AWS_ENDPOINT_URL_S3 / AWS_REGION / BUCKET_NAME, with ARTIFACT_S3_* overrides; TTL env: RUN_RESULT_TTL, PRESIGNED_URL_TTL (or ARTIFACT_S3_PRESIGN_TTL), ARTIFACT_S3_OBJECT_TTL. (Confirm the EXACT names finalized in plan 02's config.go before writing — they must match.)
The S3Store's EnsureLifecycle (plan 02) is responsible for creating the bucket if absent (BucketExists+MakeBucket) — so neither compose nor Fly needs a separate bucket-create step.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MinIO in dev compose + worker S3 env + .env.example documentation</name>
  <files>docker-compose.yml, .env.example</files>
  <read_first>
    - docker-compose.yml (redis service 26-35 for the service shape; worker service 120-160 for the env block + depends_on)
    - .env.example (lines 1-45 — block + per-var comment convention)
    - internal/config/config.go (the FINAL env var names + defaults wired in plan 02 — read so the compose/env values match exactly)
    - internal/artifactstore/s3.go (plan 02 — confirm EnsureLifecycle creates the bucket, so compose needs NO mc/init-container)
    - .planning/phases/09-artifacts-pullable-run-output/09-PATTERNS.md (section "docker-compose.yml + .env.example" — healthchecked service, documented env block)
    - .planning/phases/09-artifacts-pullable-run-output/09-SPEC.md (R14 target + acceptance; Constraints — three TTLs, ordering, ephemeral-handoff, 1-day granularity)
  </read_first>
  <action>
docker-compose.yml: add a `minio` service — pinned MinIO image (e.g. minio/minio:RELEASE.<pinned>), command `server /data --console-address ":9001"`, environment MINIO_ROOT_USER + MINIO_ROOT_PASSWORD (default to dev values via ${...:-}), `restart: unless-stopped`, `networks: [code-runner]`, a healthcheck hitting `/minio/health/live` (curl or the mc-style check; use the documented MinIO health endpoint), and a named volume for `/data` (declare it under a top-level `volumes:` key). Optionally expose 9000/9001 to host for dev console (commented like soketi's optional ports). Then wire the worker service environment: `AWS_ENDPOINT_URL_S3: http://minio:9000`, `BUCKET_NAME: ${BUCKET_NAME:-code-runner-artifacts}`, `AWS_ACCESS_KEY_ID: ${MINIO_ROOT_USER:-...}`, `AWS_SECRET_ACCESS_KEY: ${MINIO_ROOT_PASSWORD:-...}`, `AWS_REGION: ${AWS_REGION:-us-east-1}`, plus the three TTL vars (use the exact names from plan-02 config.go). Add `minio` to the worker's depends_on (with condition: service_healthy).
DO NOT add any `mc`/createbuckets init container: the worker's S3Store.EnsureLifecycle (plan 02) calls BucketExists+MakeBucket on boot, so the bucket is created automatically on a fresh MinIO. Add a short comment in the worker/minio compose section noting "bucket auto-created by the worker on boot (S3Store.EnsureLifecycle)".
.env.example: add a `# ── Artifacts / object storage ──` block documenting (one commented line per var): AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_ENDPOINT_URL_S3, AWS_REGION, BUCKET_NAME, the ARTIFACT_S3_* overrides, RUN_RESULT_TTL (~600s), the presigned-URL expiry (24h), ARTIFACT_S3_OBJECT_TTL (days). Document the ordering invariant (object TTL ≥ presigned-URL TTL — boot fails fast otherwise), the ephemeral-handoff lifecycle (consumer copies artifacts to its own store; code-runner relies on bucket lifecycle for cleanup), the 1-day lifecycle-granularity caveat (sub-day cleanup is not achievable; object TTL is expressed in days), and a note that the bucket is auto-created by the worker (no manual bucket-create step needed for a fresh MinIO/Tigris).
  </action>
  <verify>
    <automated>cd /Users/teovillanueva/code-runner && docker compose config >/dev/null && grep -q 'minio' docker-compose.yml && grep -q 'AWS_ENDPOINT_URL_S3' .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config` exits 0 (valid compose with the minio service + named volume).
    - `grep -q 'minio/minio' docker-compose.yml` succeeds with a pinned tag (not `:latest`).
    - The worker service env includes `AWS_ENDPOINT_URL_S3: http://minio:9000` and `BUCKET_NAME`; `minio` appears in the worker depends_on.
    - There is NO `mc`/createbuckets init container in compose (`grep -c 'createbuckets\|minio/mc' docker-compose.yml` returns 0) — bucket creation is the worker's job; a comment notes the bucket is auto-created on boot.
    - `.env.example` contains AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_ENDPOINT_URL_S3, AWS_REGION, BUCKET_NAME, ARTIFACT_S3_OBJECT_TTL, and a comment mentioning both "1 day" granularity and "object TTL ≥ presigned" ordering and the auto-created-bucket note.
    - The TTL/var names in compose + .env.example match the names parsed in internal/config (no typo drift).
    - (Integration, when MinIO is running) `docker compose --profile <whatever-the-worker-uses> up minio` + the plan-02 S3Store integration test proves a FRESH MinIO (no pre-created bucket) yields a working upload→presign→fetch (the EnsureLifecycle MakeBucket path) — R14 end-to-end.
  </acceptance_criteria>
  <done>MinIO runs in dev compose (pinned, healthchecked, volume) wired to the worker's S3 env with NO init-container bucket-create (the worker auto-creates the bucket via EnsureLifecycle); .env.example documents every new var + the ordering invariant + ephemeral-handoff + 1-day-granularity caveat + auto-created-bucket note; compose config validates; a fresh MinIO works end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: Fly/Tigris worker wiring + deploy doc</name>
  <files>deploy/fly/worker/fly.toml, docs/deploy-fly.md</files>
  <read_first>
    - deploy/fly/worker/fly.toml (the whole file — [env] block 13-30 + the `# Secrets` comment list convention)
    - docs/deploy-fly.md (the whole file — read it to match its prose style + per-service structure; it pairs 1:1 with the fly.toml files)
    - internal/config/config.go (the FINAL S3 env var names from plan 02 — Tigris injects AWS_*; confirm names match)
    - .planning/phases/09-artifacts-pullable-run-output/09-PATTERNS.md (section "deploy/fly/worker/fly.toml + docs/deploy-fly.md" — secrets vs [env] split, API needs none)
    - .planning/phases/09-artifacts-pullable-run-output/09-SPEC.md (R16 target + acceptance)
  </read_first>
  <action>
deploy/fly/worker/fly.toml: add the non-secret TTL config to the `[env]` block (RUN_RESULT_TTL, the presigned-URL expiry var, ARTIFACT_S3_OBJECT_TTL — same names as config.go) like WORKER_MAX_SANDBOXES. Add the Tigris-injected credentials to the `# Secrets (fly secrets set ...)` comment list: BUCKET_NAME, AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_ENDPOINT_URL_S3, AWS_REGION — note `fly storage create` sets these automatically as secrets on the worker app (do NOT put them in [env]). Add a comment that the API app needs NO S3 creds (only the worker uploads/signs/lifecycles) and that the worker auto-creates the Tigris bucket on boot if absent (S3Store.EnsureLifecycle, plan 02).
docs/deploy-fly.md: in the same prose style as the existing doc, add a section for the worker covering: run `fly storage create` to provision a Tigris bucket (it injects BUCKET_NAME + the AWS_* secrets onto the app), a credential-mapping table (Tigris injected var → S3Store env var, 1:1 since both use AWS_* — note ARTIFACT_S3_* overrides exist if names ever differ), the non-secret TTL vars to set in fly.toml [env] with the ordering invariant + 1-day-granularity caveat, an explicit note that the same S3Store code runs unmodified on Tigris (proven against MinIO in plan 02/R14) AND that the worker creates the bucket on boot if it does not exist (so even a bucket-less Tigris store works), and that the API needs no S3 secrets.
  </action>
  <verify>
    <automated>cd /Users/teovillanueva/code-runner && grep -q 'ARTIFACT_S3_OBJECT_TTL' deploy/fly/worker/fly.toml && grep -q 'fly storage create' docs/deploy-fly.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'fly storage create' docs/deploy-fly.md` succeeds and the doc has a Tigris→S3Store credential-mapping table.
    - `deploy/fly/worker/fly.toml` lists BUCKET_NAME + AWS_ACCESS_KEY_ID + AWS_SECRET_ACCESS_KEY + AWS_ENDPOINT_URL_S3 + AWS_REGION in the `# Secrets` comment block (NOT in [env]).
    - The non-secret TTL vars appear in the worker fly.toml `[env]` block.
    - The doc states the API app needs no S3 creds (only the worker) and that the worker auto-creates the bucket on boot.
    - The env var names in fly.toml match internal/config (no drift).
  </acceptance_criteria>
  <done>Worker fly.toml carries non-secret TTLs in [env] + Tigris creds in the secrets list; deploy-fly.md documents fly storage create + the credential mapping + the unmodified-S3Store-on-Tigris + auto-created-bucket claims + the API-needs-no-S3 note (R16).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env/secrets → worker | S3 creds reach only the worker app (the uploader/signer); the API app holds none |
| MinIO (dev) / Tigris (prod) → worker | Object storage backend; same S3-compatible env contract; worker auto-creates the bucket on boot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-21 | Information disclosure | S3 secrets in version control / wrong app | mitigate | Tigris creds live as Fly SECRETS on the worker (never [env], never committed); .env.example ships only dev placeholder values; the API app is documented as needing no S3 creds (R16) |
| T-09-22 | Denial of service | unbounded object accumulation in prod | mitigate | Bucket lifecycle expiration (set by S3Store on boot, plan 02/R15) is documented with the days-granular object TTL in .env.example + deploy doc; ephemeral-handoff model documented |
| T-09-23 | Tampering | TTL/var name drift between infra and code | mitigate | Both tasks read internal/config for the exact names; acceptance asserts the names match config.go (no typo drift breaking the S3Store at runtime) |
| T-09-24 | Denial of service | fresh backend with no bucket → uploads fail (R14 can't pass) | mitigate | Bucket creation is owned by the worker's S3Store.EnsureLifecycle (plan 02, BucketExists+MakeBucket); compose adds NO init container; Task 1 acceptance proves a fresh MinIO works end-to-end |
| T-09-SC | Tampering | MinIO image pin | mitigate | minio/minio pinned to a release tag (not :latest) in compose — Task 1 acceptance forbids :latest |
</threat_model>

<verification>
- `docker compose config` validates (minio + worker wiring, no init container).
- `grep` gates confirm .env.example, fly.toml, and deploy-fly.md documentation.
- Env var names consistent across compose / .env.example / fly.toml / internal/config.
- A fresh MinIO (no pre-created bucket) yields a working upload→presign→fetch via the worker's EnsureLifecycle MakeBucket path (plan-02 S3Store integration test against the dev MinIO) — R14 end-to-end.
</verification>

<success_criteria>
MinIO runs in dev compose wired to the worker (bucket auto-created by the worker, no init container); .env.example fully documents the S3 vars + three TTLs + invariants/caveats + auto-created-bucket note; Fly worker config + deploy doc wire Tigris with the same S3Store code, API needing no S3 creds; a fresh MinIO/Tigris works end-to-end (R14, R16).
</success_criteria>

<output>
Create `.planning/phases/09-artifacts-pullable-run-output/09-07-SUMMARY.md` when done.
</output>
