---
phase: 09-artifacts-pullable-run-output
plan: 05
type: execute
wave: 4
depends_on: ["09-01", "09-04"]
files_modified:
  - apps/api/src/routes/jobs.ts
  - apps/api/test/output-route.test.ts
autonomous: true
requirements: [R9]
must_haves:
  truths:
    - "GET /v1/jobs/:id/output returns the persisted RunResult (200) for a collected job"
    - "It returns 404 for an unknown id, a non-collected job, and a job past its result TTL (all leave the key absent)"
    - "It returns 401 without a valid bearer token (central /v1/* bearer middleware)"
    - "The route reads Redis only — it never calls the worker"
  artifacts:
    - path: "apps/api/src/routes/jobs.ts"
      provides: "GET /v1/jobs/:id/output handler reading keys.jobOutput"
      contains: "jobOutput"
  key_links:
    - from: "apps/api/src/routes/jobs.ts"
      to: "Redis key job:<id>:output"
      via: "redis.get(keys.jobOutput(jobId)) — Redis-only, no worker call"
      pattern: "keys.jobOutput"
---

<objective>
Add the bearer-authenticated `GET /v1/jobs/:id/output` route that returns the persisted `RunResult` (200) read from `job:<id>:output` in Redis, 404 when the key is absent (unknown / not-collected / expired all collapse to absent), and 401 without a valid bearer — mirroring the existing `GET /v1/jobs/:id` status route exactly (Redis-only, never calls the worker).

Purpose: This is the server-side pull surface (R9) that lets the edalef backend fetch full output + artifacts without subscribing to soketi.

Output: A new handler inside `registerJobsRoutes` (no new file) + a route test covering 200/404/401.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-artifacts-pullable-run-output/09-SPEC.md
@.planning/phases/09-artifacts-pullable-run-output/09-CONTEXT.md
@.planning/phases/09-artifacts-pullable-run-output/09-PATTERNS.md

<interfaces>
<!-- The exact route shape to clone. -->

From apps/api/src/routes/jobs.ts (the whole file, 1-27):
  registerJobsRoutes(app) has GET /v1/jobs/:id:
    const jobId = c.req.param("id"); const redis = getRedis();
    const statusJson = await redis.get(keys.jobStatus(jobId));
    if (!statusJson) return c.json({ error: `Job not found: ${jobId}` }, 404);
    try { JSON.parse } catch { return c.json({ error: ... }, 500); }
    return c.json(status, 200);
  keys imported from "@teovilla/code-runner-contract" — keys.jobOutput added in plan 01.

From apps/api/src/app.ts (line ~28): app.use("/v1/*", bearerAuth) applies bearer to ALL /v1/* — the new route needs NO per-handler auth (R9 401 is automatic).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET /v1/jobs/:id/output (Redis-only, 404/401)</name>
  <files>apps/api/src/routes/jobs.ts, apps/api/test/output-route.test.ts</files>
  <behavior>
    - GET /v1/jobs/:id/output with a valid bearer + a present job:<id>:output key returns 200 with the parsed RunResult JSON.
    - GET with a valid bearer + absent key returns 404 (covers unknown, not-collected, expired).
    - GET without a bearer token returns 401 (central middleware).
    - A malformed (non-JSON) stored value returns 500.
  </behavior>
  <read_first>
    - apps/api/src/routes/jobs.ts (the entire file 1-27 — the GET /v1/jobs/:id handler to clone, the keys import, getRedis import)
    - apps/api/src/app.ts (the app.use("/v1/*", bearerAuth) line — confirm central auth covers the new route)
    - apps/api/test/ (the existing route-test convention — find how jobs/status routes are tested, the test harness + how Redis is faked/seeded; copy that exactly)
    - .planning/phases/09-artifacts-pullable-run-output/09-PATTERNS.md (section "apps/api/src/routes/jobs.ts" — byte-for-byte clone against keys.jobOutput, 404 single-check, central bearer)
    - .planning/phases/09-artifacts-pullable-run-output/09-SPEC.md (R9 target + acceptance: 200/404 unknown-noncollected-expired/401)
  </read_first>
  <action>
In registerJobsRoutes(app), add a second handler `app.get("/v1/jobs/:id/output", async (c) => {...})` that is the GET /v1/jobs/:id handler cloned against `keys.jobOutput(jobId)` instead of `keys.jobStatus(jobId)`: read the key, `if (!outputJson) return c.json({ error: \`Job output not found: ${jobId}\` }, 404)` (single absence check covers unknown/not-collected/expired), parse with the same try/catch → 500 on malformed, return `c.json(runResult, 200)`. Do NOT add per-handler auth (central /v1/* bearer already applies — R9 401). Do NOT call the worker (Redis GET only — API-11 invariant). Register inside the existing function (no new route file).
Add apps/api/test/output-route.test.ts mirroring the existing route test: seed Redis (or its fake) with a RunResult at keys.jobOutput("job1"), assert 200 + body; assert 404 for a missing key; assert 401 with no/invalid bearer; assert 500 for a malformed stored value.
  </action>
  <verify>
    <automated>cd /Users/teovillanueva/code-runner && pnpm --filter @teovilla/code-runner-api test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'jobOutput' apps/api/src/routes/jobs.ts` succeeds and the new handler path is `/v1/jobs/:id/output`.
    - The handler contains NO call to the worker and NO per-handler bearer code (relies on central middleware) — source inspection confirms only `redis.get(keys.jobOutput(...))`.
    - `pnpm --filter @teovilla/code-runner-api test` passes including output-route.test.ts.
    - The test proves: 200 (present), 404 (absent), 401 (no bearer), 500 (malformed).
  </acceptance_criteria>
  <done>GET /v1/jobs/:id/output returns the RunResult (200) / 404 absent / 401 unauthenticated / 500 malformed, Redis-only, no worker call; route test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream caller → API /v1/* | Bearer-authenticated; the only trusted entry for pulling output |
| API → Redis | The API reads the persisted RunResult; it never reaches the worker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-15 | Information disclosure | leaking job existence to unauthorized callers | mitigate | The route returns 404 (not 403) for unknown/non-collected/expired — absence is indistinguishable, so a caller cannot probe which job IDs exist; 401 for missing bearer via central middleware (R9) |
| T-09-16 | Spoofing | unauthenticated output pull | mitigate | app.use("/v1/*", bearerAuth) covers the route automatically; Task 1 asserts 401 without a token |
| T-09-17 | Elevation of privilege | API calling the worker / widening coupling | mitigate | Handler is Redis-GET-only (API-11 invariant preserved); source inspection confirms no worker call |
</threat_model>

<verification>
- `pnpm --filter @teovilla/code-runner-api test` green (200/404/401/500).
- `pnpm -r test` still green overall.
- No new route file; the handler lives in registerJobsRoutes.
</verification>

<success_criteria>
A bearer-authenticated, Redis-only GET /v1/jobs/:id/output returns the persisted RunResult (200), 404 for unknown/non-collected/expired, 401 unauthenticated, never calling the worker (R9).
</success_criteria>

<output>
Create `.planning/phases/09-artifacts-pullable-run-output/09-05-SUMMARY.md` when done.
</output>
