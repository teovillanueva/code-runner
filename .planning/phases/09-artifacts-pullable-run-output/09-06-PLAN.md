---
phase: 09-artifacts-pullable-run-output
plan: 06
type: execute
wave: 4
depends_on: ["09-01", "09-05"]
files_modified:
  - packages/code-runner-sdk-node/src/client.ts
  - packages/code-runner-sdk-node/test/get-output.test.ts
  - packages/code-runner-react/src/useCodeRunnerJob.ts
autonomous: true
requirements: [R12, R13]
must_haves:
  truths:
    - "CodeRunnerClient.getOutput(id) returns a typed RunResult (with typed Artifact[]) from GET /v1/jobs/:id/output, sending the bearer"
    - "A 404 from getOutput surfaces as NotFoundError"
    - "useCodeRunnerJob exposes an artifacts: Artifact[] array, populated from soketi artifact events, non-empty at job end for a plot-producing job"
    - "The browser never needs the bearer token — it fetches each artifact url directly"
  artifacts:
    - path: "packages/code-runner-sdk-node/src/client.ts"
      provides: "getOutput(id): Promise<RunResult>"
      contains: "getOutput"
    - path: "packages/code-runner-react/src/useCodeRunnerJob.ts"
      provides: "artifacts state + onArtifact handler bound to EVENTS.artifact"
      contains: "artifacts"
  key_links:
    - from: "packages/code-runner-sdk-node/src/client.ts getOutput"
      to: "GET /v1/jobs/:id/output"
      via: "this.request<RunResult>(GET, /v1/jobs/:id/output) — bearer auto-attached, 404→NotFoundError central mapping"
      pattern: "/output"
    - from: "packages/code-runner-react/src/useCodeRunnerJob.ts"
      to: "soketi artifact event"
      via: "ch.bind(EVENTS.artifact, onArtifact) accumulating Artifact[]"
      pattern: "EVENTS.artifact"
---

<objective>
Expose the new pull surface to consumers: add `getOutput(id): Promise<RunResult>` to the Node SDK (typed, bearer-auto-attached, 404→NotFoundError), and surface `artifacts: Artifact[]` in the React `useCodeRunnerJob` hook by collecting the soketi `artifact` events so the browser path can render plots at job end (fetching each presigned `url` directly, no bearer).

Purpose: R12/R13 make the persisted result and the interactive artifact stream usable in one typed call (server) and one hook field (browser).

Output: One SDK method + test; one hook state field + event binding. Both depend on the contract types (plan 01) and, for the SDK test, the route shape (plan 05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-artifacts-pullable-run-output/09-SPEC.md
@.planning/phases/09-artifacts-pullable-run-output/09-CONTEXT.md
@.planning/phases/09-artifacts-pullable-run-output/09-PATTERNS.md

<interfaces>
<!-- The exact patterns to clone. -->

From packages/code-runner-sdk-node/src/client.ts:
  getJob(id): Promise<JobStatus> { return this.request<JobStatus>("GET", `/v1/jobs/${encodeURIComponent(id)}`); }
  request<T>() (107+) auto-attaches `Authorization: Bearer ${this.token}` and maps statuses (throwForStatus: 404→NotFoundError already wired). Imports JobStatus etc. from "@teovilla/code-runner-contract" (7-12).

From packages/code-runner-react/src/useCodeRunnerJob.ts:
  EVENTS const (19-24): { stage, stdout, stderr, result }.
  UseCodeRunnerJobResult (38-46): stage/stdout/stderr/result/status/sendStdin/kill.
  useEffect (75-118): reset block (77-83), ch.bind(EVENTS.x, handler) for each event, cleanup unbinds + unsubscribe. onResult sets result + status "done".
  Imports types from "@teovilla/code-runner-contract" (8-13).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Node SDK getOutput(id): Promise&lt;RunResult&gt;</name>
  <files>packages/code-runner-sdk-node/src/client.ts, packages/code-runner-sdk-node/test/get-output.test.ts</files>
  <behavior>
    - getOutput("job1") issues GET /v1/jobs/job1/output with an Authorization: Bearer header and returns the parsed RunResult (typed; artifacts is Artifact[]).
    - A 404 response causes getOutput to reject with NotFoundError.
  </behavior>
  <read_first>
    - packages/code-runner-sdk-node/src/client.ts (getJob 69-72, request<T> 107+, the contract import block 7-12)
    - packages/code-runner-sdk-node/src/errors.ts (NotFoundError + throwForStatus 404 mapping)
    - packages/code-runner-sdk-node/test/ (the existing client test convention — how getJob/execute are tested with a fake FetchLike; copy it)
    - .planning/phases/09-artifacts-pullable-run-output/09-PATTERNS.md (section "packages/code-runner-sdk-node/src/client.ts" — typed GET helper, central error/auth)
    - .planning/phases/09-artifacts-pullable-run-output/09-SPEC.md (R12 acceptance)
  </read_first>
  <action>
Import `RunResult` from "@teovilla/code-runner-contract" alongside the existing JobStatus/etc. imports. Add a method `getOutput(id: string): Promise<RunResult> { return this.request<RunResult>("GET", \`/v1/jobs/${encodeURIComponent(id)}/output\`); }` with a doc comment matching getJob's style (`/** GET /v1/jobs/:id/output — fetch the persisted run output. 404 -> NotFoundError. */`). No extra error or auth code — request() attaches the bearer and throwForStatus maps 404→NotFoundError centrally.
Add packages/code-runner-sdk-node/test/get-output.test.ts modelled on the existing getJob test: inject a fake FetchLike returning a 200 RunResult JSON → assert the typed result + that the Authorization header was sent; a fake returning 404 → assert rejection with NotFoundError.
  </action>
  <verify>
    <automated>cd /Users/teovillanueva/code-runner && pnpm --filter @teovilla/code-runner-sdk-node test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'getOutput' packages/code-runner-sdk-node/src/client.ts` succeeds and the path is `/v1/jobs/${encodeURIComponent(id)}/output`.
    - `grep -q 'RunResult' packages/code-runner-sdk-node/src/client.ts` succeeds (typed return).
    - `pnpm --filter @teovilla/code-runner-sdk-node test` passes including get-output.test.ts.
    - The test asserts the bearer header is sent AND that a 404 rejects with NotFoundError.
  </acceptance_criteria>
  <done>getOutput(id) returns a typed RunResult over GET /v1/jobs/:id/output with the bearer auto-attached and 404→NotFoundError; SDK test green.</done>
</task>

<task type="auto">
  <name>Task 2: React hook exposes artifacts[] from soketi artifact events</name>
  <files>packages/code-runner-react/src/useCodeRunnerJob.ts</files>
  <read_first>
    - packages/code-runner-react/src/useCodeRunnerJob.ts (EVENTS 19-24, UseCodeRunnerJobResult 38-46, useEffect reset 77-83 + binds 106-109 + cleanup 111-117, returned object 126, contract import 8-13)
    - .planning/phases/09-artifacts-pullable-run-output/09-PATTERNS.md (section "packages/code-runner-react/src/useCodeRunnerJob.ts" — bind event → accumulate state → unbind)
    - .planning/phases/09-artifacts-pullable-run-output/09-SPEC.md (R13 acceptance — non-empty artifacts at result, each carrying name/mimeType/bytes/url, browser never needs bearer)
  </read_first>
  <action>
Import `Artifact` from "@teovilla/code-runner-contract" alongside the existing type imports. Add `artifact: "artifact"` to the local EVENTS const (mirrors contract events.artifact). Add `const [artifacts, setArtifacts] = useState<Artifact[]>([])`. In the useEffect reset block, add `setArtifacts([])`. Add `const onArtifact = (data: Artifact) => setArtifacts((prev) => [...prev, data])`, bind it with `ch.bind(EVENTS.artifact, onArtifact)`, and unbind it in the cleanup. Add `artifacts: Artifact[]` to the UseCodeRunnerJobResult interface and include `artifacts` in the returned object. The browser fetches each `artifact.url` directly — add NO bearer-token handling (trust boundary unchanged, R13).
  </action>
  <verify>
    <automated>cd /Users/teovillanueva/code-runner && pnpm --filter @teovilla/code-runner-react build && grep -q 'EVENTS.artifact' packages/code-runner-react/src/useCodeRunnerJob.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'artifact: "artifact"' packages/code-runner-react/src/useCodeRunnerJob.ts` succeeds (EVENTS updated).
    - `grep -q 'artifacts: Artifact\[\]' packages/code-runner-react/src/useCodeRunnerJob.ts` succeeds (interface field).
    - `grep -q 'ch.bind(EVENTS.artifact' packages/code-runner-react/src/useCodeRunnerJob.ts` AND a matching `ch.unbind(EVENTS.artifact` both succeed (bound + cleaned up).
    - `setArtifacts([])` appears in the reset block; `artifacts` is in the returned object.
    - No bearer/token reference is added to the hook (`grep -ci 'token\|bearer\|authorization' packages/code-runner-react/src/useCodeRunnerJob.ts` returns 0).
    - `pnpm --filter @teovilla/code-runner-react build` exits 0 (typecheck passes).
  </acceptance_criteria>
  <done>useCodeRunnerJob collects soketi artifact events into an artifacts: Artifact[] state (reset per job, bound + unbound), returned in the hook result; browser fetches urls directly with no bearer; package builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Node SDK (server) → API | Holds the bearer; pulls RunResult server-side |
| browser hook → soketi + presigned URLs | Receives artifact metadata over soketi; fetches bytes via presigned URLs with NO bearer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-18 | Information disclosure | bearer token leaking into the browser | mitigate | The React hook adds NO token handling; it fetches presigned urls directly (R13 trust boundary unchanged) — Task 2 acceptance asserts zero token/bearer references |
| T-09-19 | Spoofing | SDK calling the route without auth | accept | The SDK's request() always attaches the bearer (existing, unchanged); getOutput delegates to it — no new auth path introduced |
| T-09-20 | Tampering | soketi artifact event treated as authoritative | accept | The hook surfaces artifacts for UX only; the authoritative source is the pulled RunResult (best-effort soketi delivery is documented in SPEC out-of-scope). No integrity guarantee needed for the convenience stream. |
</threat_model>

<verification>
- `pnpm --filter @teovilla/code-runner-sdk-node test` green.
- `pnpm --filter @teovilla/code-runner-react build` green.
- `pnpm -r test` / `pnpm -r build` still green overall.
</verification>

<success_criteria>
Node SDK getOutput returns a typed RunResult (bearer, 404→NotFoundError); React hook exposes a populated artifacts[] from soketi artifact events with no browser bearer (R12, R13).
</success_criteria>

<output>
Create `.planning/phases/09-artifacts-pullable-run-output/09-06-SUMMARY.md` when done.
</output>
