---
phase: 09-artifacts-pullable-run-output
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - languages/python-3.12/Dockerfile
  - languages/r-4.4/Dockerfile
autonomous: true
requirements: [R10, R11]
must_haves:
  truths:
    - "A Python script doing import matplotlib.pyplot; plt.plot(...); plt.savefig('plot.png') produces plot.png in /workspace with no network"
    - "numpy/pandas/requests still import in the python image"
    - "An R script doing png('chart.png'); plot(1:3); dev.off() produces chart.png in /workspace with no popen/seccomp stderr noise"
    - "existing jsonlite/data.table behavior unchanged; no --network requirement added"
    - "D-10: No per-language plot-interception shim — under headless MPLBACKEND=Agg plt.show() writes nothing and is intentionally not auto-captured; Python installs matplotlib, R reconciles R_DEFAULT_PACKAGES for a usable png() file device; documented save-to-cwd-with-relative-name convention"
  artifacts:
    - path: "languages/python-3.12/Dockerfile"
      provides: "matplotlib baked + MPLBACKEND=Agg, save-to-cwd convention documented"
      contains: "matplotlib"
    - path: "languages/r-4.4/Dockerfile"
      provides: "usable file graphics device (png) via reconciled R_DEFAULT_PACKAGES + cairo libs"
      contains: "grDevices"
  key_links:
    - from: "languages/python-3.12/Dockerfile"
      to: "workspace-diff capture (plan 04)"
      via: "headless savefig writes a relative file into cwd /workspace"
      pattern: "MPLBACKEND=Agg"
    - from: "languages/r-4.4/Dockerfile"
      to: "workspace-diff capture (plan 04)"
      via: "png() file device writes a relative file into cwd /workspace"
      pattern: "R_DEFAULT_PACKAGES"
---

<objective>
Give the Python and R sandbox images the ability to produce plot FILES that the Phase-9 workspace-diff capture (plan 04) picks up — with NO plot-interception shims (D-10). Python gets matplotlib + `MPLBACKEND=Agg`; R gets a usable file graphics device by reconciling `R_DEFAULT_PACKAGES` and adding any missing cairo/X libs, without reintroducing the popen/seccomp stderr noise.

Purpose: R10/R11 close the "users can generate plots" gap. The capture mechanism is path-agnostic — users save with a relative name (`savefig('plot.png')`, `png('chart.png'); …; dev.off()`) and the file lands in cwd `/workspace`. All deps baked at build time; sandboxes stay `--network=none`.

Output: Updated Python and R Dockerfiles. No core/worker changes (LANG-01 invariant — language = image only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-artifacts-pullable-run-output/09-SPEC.md
@.planning/phases/09-artifacts-pullable-run-output/09-CONTEXT.md
@.planning/phases/09-artifacts-pullable-run-output/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Python image — matplotlib + MPLBACKEND=Agg (no shim)</name>
  <files>languages/python-3.12/Dockerfile</files>
  <read_first>
    - languages/python-3.12/Dockerfile (the file being modified — pip install layer 32-35, ENV block 22-26, workspace 43-45)
    - .planning/phases/09-artifacts-pullable-run-output/09-PATTERNS.md (section "languages/python-3.12/Dockerfile" — extend pip layer, ENV MPLBACKEND=Agg, no shim)
    - .planning/phases/09-artifacts-pullable-run-output/09-SPEC.md (R10 target + acceptance; D-10 no plt.show() shim)
  </read_first>
  <action>
Add `matplotlib==<pinned 3.x compatible with numpy 2.2.6>` to the existing `pip install --no-cache-dir` list (alongside numpy/pandas/requests). Confirm the current matplotlib 3.x version that builds against numpy 2.2.6 via PyPI/Context7 before pinning. Add `ENV MPLBACKEND=Agg` near the existing ENV block (PYTHONUNBUFFERED / PYTHONDONTWRITEBYTECODE) so headless savefig works without a display. Do NOT add any sitecustomize / plt.show() interception shim (D-10 — show() under Agg is intentionally a no-op). Add a comment in the file's existing heavy-comment style documenting the save-to-cwd convention: plt.savefig('plot.png') writes a relative file into /workspace which the worker captures as an artifact. Keep --network=none correctness: all deps baked here, no runtime fetch.
  </action>
  <verify>
    <automated>cd /Users/teovillanueva/code-runner && make python-image && printf "import matplotlib\nmatplotlib.use('Agg')\nimport matplotlib.pyplot as plt\nimport numpy, pandas, requests\nplt.plot([1,2,3]); plt.savefig('plot.png')\nimport os; print('OK' if os.path.exists('plot.png') else 'MISSING')\n" > /tmp/cr_py_main.py && docker run --rm --network=none -v /tmp/cr_py_main.py:/workspace/main.py:ro -w /workspace executor/python:3.12 python main.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'matplotlib' languages/python-3.12/Dockerfile` succeeds (baked in pip layer).
    - `grep -q 'MPLBACKEND=Agg' languages/python-3.12/Dockerfile` succeeds.
    - No interception shim: `grep -ci 'sitecustomize\|plt.show\|usercustomize' languages/python-3.12/Dockerfile` returns 0.
    - The verify command prints `OK` — savefig produced plot.png under --network=none, and numpy/pandas/requests imported with no ImportError.
  </acceptance_criteria>
  <done>Python image bakes matplotlib + MPLBACKEND=Agg, no shim; a headless savefig produces a plot.png artifact file under --network=none with numpy/pandas/requests intact.</done>
</task>

<task type="auto">
  <name>Task 2: R image — usable png() device via reconciled R_DEFAULT_PACKAGES (no shim)</name>
  <files>languages/r-4.4/Dockerfile</files>
  <read_first>
    - languages/r-4.4/Dockerfile (the file being modified — install.packages 64-70, R_DEFAULT_PACKAGES rationale 72-86, R_DEFAULT_DEVICE=pdf line 37)
    - .planning/phases/09-artifacts-pullable-run-output/09-PATTERNS.md (section "languages/r-4.4/Dockerfile" — reconcile R_DEFAULT_PACKAGES, add cairo/X libs if needed, no auto-device shim)
    - .planning/phases/09-artifacts-pullable-run-output/09-SPEC.md (R11 target + acceptance — png()/dev.off() produces chart.png, jsonlite/data.table unchanged, no popen/seccomp noise)
  </read_first>
  <action>
Change `ENV R_DEFAULT_PACKAGES=base` to include the graphics packages the png() file device needs: `base,grDevices,graphics`. These do NOT trigger the utils popen("which uname") EPERM path that the comment block (lines 72-86) attributes specifically to `utils` — keep `utils`/`stats`/`methods` OUT to preserve the silence. Keep R_DEFAULT_DEVICE=pdf as-is (line 37) — the user explicitly opens png() so the startup default device is moot. If r-base:4.4.2 lacks the cairo/X11 libs a PNG device needs, add an `apt-get install --no-install-recommends` layer (e.g. libcairo2, libxt6) BEFORE the install.packages step, in the file's existing layered style, ending with the apt-lists cleanup the repo uses. Validate empirically that png() works under the hardened sandbox WITHOUT new stderr noise. Add NO auto-device / show()-interception shim (D-10). Document the convention in a comment: png('chart.png'); plot(...); dev.off() writes to cwd. Bake everything at build — no --network requirement.
  </action>
  <verify>
    <automated>cd /Users/teovillanueva/code-runner && make r-image && printf "png('chart.png'); plot(1:3); dev.off(); library(jsonlite); library(data.table); cat(if (file.exists('chart.png')) 'OK' else 'MISSING')\n" > /tmp/cr_r_main.R && docker run --rm --network=none -v /tmp/cr_r_main.R:/workspace/main.R:ro -w /workspace executor/r:4.4 Rscript main.R</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'R_DEFAULT_PACKAGES=base,grDevices,graphics' languages/r-4.4/Dockerfile` succeeds (or an equivalent comma list that includes grDevices and graphics but excludes utils).
    - No auto-device shim: `grep -ci 'addTaskCallback.*png\|module://\|dev.new' languages/r-4.4/Dockerfile` returns 0 for png-auto-capture machinery (the existing flush addTaskCallback at lines 50-55 is allowed and unrelated).
    - The verify command prints `OK` (chart.png created; jsonlite + data.table loaded) under --network=none.
    - The verify command emits no `pthread_create`/`popen`/`utils`/`EPERM` stderr lines attributable to the graphics change (inspect stderr — must match the pre-change baseline noise level for jsonlite/data.table runs).
  </acceptance_criteria>
  <done>R image renders png() to a file via base,grDevices,graphics (utils still excluded → no popen/EPERM noise); chart.png produced under --network=none; jsonlite/data.table unchanged; no plot shim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time deps → runtime sandbox | All plot libraries are baked at build; the runtime sandbox is --network=none so no fetch is possible |
| user code → /workspace files | Untrusted code writes plot files into the writable /workspace volume (later captured by the worker) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08 | Elevation of privilege | image deps enabling network/exec | mitigate | matplotlib/cairo are pure render libs; sandbox stays --network=none + cap-drop ALL + seccomp (unchanged). No new capabilities. Verify commands run with --network=none and still succeed. |
| T-09-09 | Denial of service | R graphics reintroducing popen/EPERM stderr flood | mitigate | R_DEFAULT_PACKAGES keeps utils OUT (the sole popen offender per the existing comment); Task 2 acceptance asserts no new popen/EPERM stderr |
| T-09-SC | Tampering | pip/apt installs (new packages) | mitigate | matplotlib is a top-tier PyPI package (pinned version, confirmed via PyPI); cairo/libxt are Debian base packages. No [SLOP]/[SUS] packages — versions pinned in the Dockerfile. |
</threat_model>

<verification>
- `make python-image` + `make r-image` build cleanly.
- The two verify commands produce plot files under --network=none.
- No worker/core/manifest changes (LANG-01 invariant): `git diff --name-only` shows only the two Dockerfiles.
</verification>

<success_criteria>
Python bakes matplotlib + MPLBACKEND=Agg; R renders png() via reconciled R_DEFAULT_PACKAGES (+ cairo libs if needed); both produce plot files in /workspace under --network=none with no shims and no new stderr noise (R10, R11).
</success_criteria>

<output>
Create `.planning/phases/09-artifacts-pullable-run-output/09-03-SUMMARY.md` when done.
</output>
