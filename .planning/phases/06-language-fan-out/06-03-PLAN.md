---
phase: 06-language-fan-out
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - languages/r-4.4/manifest.json
  - languages/r-4.4/Dockerfile
  - internal/worker/langfanout_r_test.go
autonomous: true
requirements: [LANG-07]
must_haves:
  truths:
    - "An R job runs Rscript main.R end-to-end through the worker and streams its output"
    - "An interactive R session reads a value from stdin and prints a response (unbuffered streaming works)"
    - "Adding R touched ONLY languages/r-4.4/ + one build-tagged test file (zero core changes); compile is null"
  artifacts:
    - path: "languages/r-4.4/manifest.json"
      provides: "R language package: image, run argv (Rscript), null compile, limits"
      contains: "Rscript"
    - path: "languages/r-4.4/Dockerfile"
      provides: "executor/r:4.4 image with common libs baked in"
      contains: "4.4"
    - path: "internal/worker/langfanout_r_test.go"
      provides: "build-tagged end-to-end Rscript + interactive stdin integration test"
  key_links:
    - from: "languages/r-4.4/manifest.json"
      to: "JobSpec.Run"
      via: "manifest run [\"Rscript\",\"main.R\"] consumed by the worker run path"
      pattern: "Rscript"
---

<objective>
Add R 4.4 as a pure language package — `languages/r-4.4/{manifest.json, Dockerfile}` —
that runs `Rscript main.R` with common libs baked in and streams output unbuffered for
the interactive session model. Prove it end-to-end through the worker (batch Rscript +
an interactive stdin round-trip).

Purpose: LANG-07 — the R 4.4 package runs `Rscript main.R` driven entirely by its
manifest, with zero changes to the worker or API. compile is null.

Output: the R language folder + a build-tagged integration test that builds the image
and runs real jobs through the worker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-language-fan-out/06-CONTEXT.md
@.planning/phases/06-language-fan-out/06-01-SUMMARY.md

<read_first>
- languages/python-3.12/manifest.json — reference manifest (compile:null path, like R)
- languages/python-3.12/Dockerfile — reference image conventions (no USER/CMD; /workspace 1777; PYTHONUNBUFFERED is the streaming-buffering precedent — R needs the equivalent)
- internal/worker/integration_test.go — the worker harness to mirror (full queued→start→running→stdin→result flow, publishStdinRaw, assertNoContainerLeak)
- internal/worker/worker.go (post-06-01) — run path; compile gate is skipped when compile is null (R parity with Python)
- packages/contract/gen/go/wire/wire.gen.go — JobSpec fields, StagePhaseRunning, ResultEvent
- Makefile — r-image + langfanout targets (pre-created in 06-01)
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: R 4.4 language package (manifest + Dockerfile)</name>
  <files>languages/r-4.4/manifest.json, languages/r-4.4/Dockerfile</files>
  <read_first>languages/python-3.12/manifest.json, languages/python-3.12/Dockerfile</read_first>
  <action>
    Create languages/r-4.4/manifest.json mirroring the Python field set: language "r",
    version "4.4", aliases ["rlang"], image "executor/r:4.4", entrypoint "main.R",
    compile null, run ["Rscript","main.R"], interactive true, and defaultLimits with
    every field >= 1 (wallTimeMs at least 30000, idleMs at least 10000, cpuMs at least
    15000, memoryMb at least 256 since R has a larger baseline footprint than Python,
    pids at least 64, outputKb at least 512).

    Create languages/r-4.4/Dockerfile FROM r-base:4.4.x (pin a concrete 4.4 patch tag,
    e.g. r-base:4.4.2) or rocker/r-ver:4.4.x. Follow the Python image conventions: no
    USER, no CMD/ENTRYPOINT (the runner supplies --user 65534:65534 + argv). Create
    /workspace with chmod 1777 and WORKDIR /workspace. Preinstall a few common CRAN libs
    at build time (since runtime is --network=none, all deps MUST be baked) — choose a
    small, broadly useful set (e.g. jsonlite, data.table) via a non-interactive
    Rscript -e install.packages call pinned to a CRAN snapshot/repo. Address streaming
    buffering: R buffers stdout when not attached to a TTY, which would stall interactive
    output the same way Python does without PYTHONUNBUFFERED — set the equivalent so
    output flushes per line (e.g. an R environment/profile that disables output buffering,
    or rely on Rscript line-buffering + explicit flush). Document the buffering choice in
    a Dockerfile comment the way the Python image documents PYTHONUNBUFFERED.
  </action>
  <verify>
    <automated>docker build -t executor/r:4.4 languages/r-4.4 &amp;&amp; node -e "const m=require('./languages/r-4.4/manifest.json'); if(m.compile!==null||m.run[0]!=='Rscript'||m.run[1]!=='main.R')process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `docker build -t executor/r:4.4 languages/r-4.4` succeeds (large image build is expected and fine).
    - manifest.json: compile is null, run is ["Rscript","main.R"], entrypoint "main.R", all six defaultLimits fields >= 1.
    - Dockerfile FROM a pinned r-base:4.4.x (or rocker/r-ver:4.4.x), no USER/CMD, /workspace 1777, common CRAN libs baked, a documented unbuffered-streaming setting.
    - Only files under languages/r-4.4/ were created.
  </acceptance_criteria>
  <done>executor/r:4.4 builds with common libs baked and a documented unbuffered-output setting; manifest runs Rscript main.R with null compile.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end R integration test (Rscript batch + interactive stdin)</name>
  <files>internal/worker/langfanout_r_test.go</files>
  <read_first>internal/worker/integration_test.go, internal/worker/worker.go, packages/contract/gen/go/wire/wire.gen.go</read_first>
  <action>
    Create internal/worker/langfanout_r_test.go guarded with `//go:build langfanout`
    in package worker_test, run name prefix TestLangFanout_R... . Redeclare the small
    harness helpers it needs scoped so they do not clash with the Rust file under the
    same tag (since 06-02 and 06-03 both build under `langfanout`, put shared helpers in
    EXACTLY ONE of the langfanout files and reference them from the other, OR give each
    file uniquely-named helpers — pick uniquely-named per-file helpers, e.g.
    rDialRedis/rDockerGuard, to guarantee no duplicate-symbol error when both files
    compile together). Skip cleanly if Redis/Docker/the executor/r:4.4 image is missing.

    Case A (Rscript batch): enqueue a JobSpec for language "r" version "4.4", image
    "executor/r:4.4", compile nil, run ["Rscript","main.R"], a Files entry main.R that
    prints a known token (e.g. cat("hi r\n")), interactive true. Drive enqueue → worker
    loop → start → assert NO `compiling` stage is published (compile is null) → assert a
    `running` stage → assert a stdout event contains the token → assert terminal result
    ExitCode 0. Assert no container leak. Log captured events.

    Case B (interactive stdin): main.R reads one line from stdin (e.g. via readLines("stdin",
    n=1) or file("stdin")) and prints a response incorporating it. Drive the full
    interactive flow mirroring the Python interactive test: wait for `running`, send a
    stdin chunk via the publishStdin helper (JSON StdinMessage format), assert a stdout
    event contains the echoed value (proving unbuffered streaming + stdin routing work
    for R), then send stdin_close / let it reach EOF, and assert a terminal result.
    Assert no container leak.
  </action>
  <verify>
    <automated>go vet -tags=langfanout ./internal/worker/... &amp;&amp; go test ./... &amp;&amp; make langfanout 2>&amp;1 | grep -E "TestLangFanout_R.*(PASS|ok|SKIP)"</automated>
  </verify>
  <acceptance_criteria>
    - File compiles under `-tags=langfanout` alongside the Rust file with no duplicate-symbol errors; excluded from plain `go test ./...` (default suite stays green).
    - `make langfanout` (with redis:7 + Docker + executor/r:4.4 built) runs the R cases: Rscript batch prints the token with terminal ExitCode 0 and NO `compiling` stage; the interactive case echoes a stdin value to stdout (streaming proven) and reaches a terminal result.
    - assertNoContainerLeak passes for both cases.
    - Plan diff confined to languages/r-4.4/ + internal/worker/langfanout_r_test.go.
  </acceptance_criteria>
  <done>A real Rscript job and an interactive R stdin round-trip run through the worker with zero core changes; unbuffered streaming confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| submitted main.R → Rscript inside sandbox | Untrusted R script executed by Rscript in the hardened sandbox |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Denial of Service | hostile R script (loop / alloc) | mitigate | run governed by the existing three clocks + tree-kill + memory cap (unchanged core); manifest limits sized for R's baseline |
| T-06-03-02 | Tampering | baked CRAN packages | accept | small set installed at build time from CRAN with --network=none at runtime; offline CRAN vendoring beyond baked libs is deferred to v2 |
| T-06-03-SC | Tampering | r-base / rocker base image + CRAN install | mitigate | base image is a well-known official/rocker image with a pinned 4.4.x tag (verifiable on hub.docker.com); CRAN packages are install-time only, no runtime fetch |
</threat_model>

<verification>
- `docker build -t executor/r:4.4 languages/r-4.4` succeeds.
- `go test ./...` stays green (langfanout test excluded by build tag).
- `make langfanout` runs the R Rscript batch + interactive stdin cases green.
- Plan diff confined to languages/r-4.4/ + internal/worker/langfanout_r_test.go.
</verification>

<success_criteria>
- LANG-07 proven end-to-end: Rscript main.R with common libs baked, null compile, plus an interactive stdin round-trip with unbuffered streaming.
- Zero core changes — adding R touched only its folder + one test file.
</success_criteria>

<output>
Create `.planning/phases/06-language-fan-out/06-03-SUMMARY.md` when done.
</output>
