---
phase: 06-language-fan-out
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - languages/rust-1.83/manifest.json
  - languages/rust-1.83/Dockerfile
  - internal/worker/langfanout_rust_test.go
autonomous: true
requirements: [LANG-06]
must_haves:
  truths:
    - "A Rust job compiles main.rs with rustc -O then runs the produced binary, end-to-end through the worker"
    - "A Rust compile-error program streams the compiler stderr and publishes a terminal result with a non-zero exit code, and never runs"
    - "Adding Rust touched ONLY languages/rust-1.83/ + one build-tagged test file (zero core changes)"
  artifacts:
    - path: "languages/rust-1.83/manifest.json"
      provides: "Rust language package: image, compile argv, run argv, limits"
      contains: "rustc"
    - path: "languages/rust-1.83/Dockerfile"
      provides: "executor/rust:1.83 image"
      contains: "rust:1.83-slim"
    - path: "internal/worker/langfanout_rust_test.go"
      provides: "build-tagged end-to-end Rust compile+run and compile-error integration test"
  key_links:
    - from: "languages/rust-1.83/manifest.json"
      to: "JobSpec.Compile"
      via: "manifest compile field consumed by the generic compile stage (06-01)"
      pattern: "\"compile\""
---

<objective>
Add Rust as a pure language package — `languages/rust-1.83/{manifest.json, Dockerfile}`
— consuming the generic compile stage delivered in 06-01. Prove end-to-end through the
worker that a Rust program compiles (`rustc -O main.rs -o /tmp/prog`) then runs the
produced binary, and that a compile-error program streams compiler stderr + yields a
non-zero terminal result with no run.

Purpose: LANG-06 — the Rust package compiles then runs the produced binary, added with
ZERO changes to the worker or API.

Output: the Rust language folder + a build-tagged integration test that builds the image
and runs real jobs through the worker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-language-fan-out/06-CONTEXT.md
@.planning/phases/06-language-fan-out/06-01-SUMMARY.md

<read_first>
- languages/python-3.12/manifest.json — the reference manifest shape (all fields, defaultLimits)
- languages/python-3.12/Dockerfile — the reference image pattern (no USER/CMD; runner sets --user 65534 + argv; /workspace 1777; --network=none means all deps baked at build)
- internal/worker/integration_test.go — the worker harness to mirror (dialTestRedis, requireDockerAndImage, integrationTriggerer, waitFor, publishStdinRaw, assertNoContainerLeak, full queued→start→running→result flow)
- internal/runner/runner.go (post-06-01) — the Sandbox.Compile signature the worker now calls
- internal/worker/worker.go (post-06-01) — the compile gate (StagePhaseCompiling, compile-failure terminal result)
- packages/contract/gen/go/wire/wire.gen.go — JobSpec.Compile (*JobSpecCompile), StagePhaseCompiling, ResultEvent.ExitCode
- Makefile — rust-image + langfanout targets (pre-created in 06-01)
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rust language package (manifest + Dockerfile)</name>
  <files>languages/rust-1.83/manifest.json, languages/rust-1.83/Dockerfile</files>
  <read_first>languages/python-3.12/manifest.json, languages/python-3.12/Dockerfile</read_first>
  <action>
    Create languages/rust-1.83/manifest.json mirroring the Python manifest field set:
    language "rust", version "1.83", aliases ["rs"], image "executor/rust:1.83",
    entrypoint "main.rs", compile ["rustc","-O","main.rs","-o","/tmp/prog"],
    run ["/tmp/prog"], interactive true. Set defaultLimits generous-but-bounded so a
    real rustc compile fits inside the wall/CPU clocks: wallTimeMs at least 60000,
    idleMs at least 10000, cpuMs at least 30000, memoryMb at least 512 (rustc is
    memory-hungry), pids at least 128, outputKb at least 512. Compile output goes to
    /tmp/prog (the writable tmpfs) and run executes exactly that path — the compile and
    run paths MUST agree. Every limits field must be >= 1 (manifest validator).

    Create languages/rust-1.83/Dockerfile FROM rust:1.83-slim. Follow the Python image
    conventions: no USER and no CMD/ENTRYPOINT (the runner supplies --user 65534:65534
    and the argv from the manifest). Create /workspace with chmod 1777 and WORKDIR
    /workspace. rustc + the standard library ship in the base image, so no extra package
    install is required for std-only programs (third-party crate vendoring is explicitly
    v2 / deferred). Ensure /tmp is writable for the compile output (the runner mounts a
    size-capped tmpfs at /tmp at run time — the manifest's outputKb-derived sizing must
    leave room for the compiled binary; if needed bump outputKb so the tmpfs is large
    enough for a small Rust binary).
  </action>
  <verify>
    <automated>docker build -t executor/rust:1.83 languages/rust-1.83 &amp;&amp; node -e "const m=require('./languages/rust-1.83/manifest.json'); if(!m.compile||m.compile[0]!=='rustc'||m.run[0]!=='/tmp/prog')process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `docker build -t executor/rust:1.83 languages/rust-1.83` succeeds.
    - manifest.json: compile starts with "rustc", run is ["/tmp/prog"] matching the compile -o path, compile is non-null, all six defaultLimits fields >= 1.
    - Dockerfile FROM rust:1.83-slim, no USER/CMD, /workspace 1777, WORKDIR /workspace.
    - Only files under languages/rust-1.83/ were created (no core/Makefile edits in this task).
  </acceptance_criteria>
  <done>executor/rust:1.83 builds and the manifest declares a valid rustc compile + produced-binary run.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end Rust integration test (compile+run + compile-error)</name>
  <files>internal/worker/langfanout_rust_test.go</files>
  <read_first>internal/worker/integration_test.go, internal/worker/worker.go, packages/contract/gen/go/wire/wire.gen.go</read_first>
  <action>
    Create internal/worker/langfanout_rust_test.go guarded with `//go:build langfanout`
    in package worker_test, run name prefix TestLangFanout_Rust... (matched by `make
    langfanout` -run LangFanout). Self-contain all harness helpers it needs (or reuse
    names only if no duplicate-symbol clash under the langfanout tag — since the
    integration/abuse harnesses live under different build tags, redeclare the small
    helpers you use: dialTestRedis, a docker-client+image guard parameterized on the
    image tag, integration event collector, waitFor, assertNoContainerLeak). Skip
    cleanly (t.Skip) if Redis/Docker/the executor/rust:1.83 image is unavailable.

    Case A (compile + run): enqueue a JobSpec for language "rust" version "1.83",
    image "executor/rust:1.83", compile ["rustc","-O","main.rs","-o","/tmp/prog"],
    run ["/tmp/prog"], a Files entry main.rs whose content is a std-only program that
    prints a known token (e.g. fn main(){ println!("hi rust"); }), interactive true,
    generous limits. Drive the full flow: enqueue → run worker loop → wait for the
    `compiling` stage event → wait for the `running` stage event → assert a stdout event
    contains the token → assert a terminal result with ExitCode 0. Assert no container
    leak. Log the captured events (paste real output per CONTEXT).

    Case B (compile error): same shape but main.rs content has a deliberate compile error
    (e.g. references an undefined symbol). Assert: a `compiling` stage is published, the
    compiler stderr is forwarded (a stderr event whose chunk contains "error"), the
    terminal result has a NON-ZERO ExitCode, and NO `running` stage is ever published.
    Assert no container leak.
  </action>
  <verify>
    <automated>go vet -tags=langfanout ./internal/worker/... &amp;&amp; go test ./... &amp;&amp; make langfanout 2>&amp;1 | grep -E "TestLangFanout_Rust.*(PASS|ok|SKIP)"</automated>
  </verify>
  <acceptance_criteria>
    - File compiles under `-tags=langfanout` and is excluded from plain `go test ./...` (default `go test ./...` stays green).
    - `make langfanout` (with redis:7 on :6381 + Docker + executor/rust:1.83 built) runs the Rust cases: compile+run prints the token with terminal ExitCode 0; the compile-error case forwards compiler stderr ("error" in a stderr chunk), yields a non-zero terminal ExitCode, and emits NO `running` stage.
    - No container with the test jobId label survives (assertNoContainerLeak passes).
    - Diff for this entire plan is confined to languages/rust-1.83/ + this one test file (prove the zero-core-change invariant).
  </acceptance_criteria>
  <done>A real Rust job compiles+runs through the worker and a compile-error job streams compiler stderr + non-zero result with no run — all with zero core changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| submitted main.rs → rustc inside sandbox | Untrusted source compiled by rustc running in the hardened sandbox |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Denial of Service | rustc compile of hostile source | mitigate | compile runs under the generic compile stage's wall/CPU clocks + tree-kill (06-01); generous-but-bounded manifest limits cap rustc |
| T-06-02-02 | Tampering | base image rust:1.83-slim | accept | official Docker Library image, pinned tag; no third-party package installs in the Dockerfile (crate vendoring deferred to v2) |
| T-06-02-SC | Tampering | docker base image pull | mitigate | rust:1.83-slim is an official, well-known image (verifiable on hub.docker.com/_/rust); no npm/pip/cargo add steps requiring a legitimacy gate |
</threat_model>

<verification>
- `docker build -t executor/rust:1.83 languages/rust-1.83` succeeds.
- `go test ./...` stays green (langfanout test excluded by build tag).
- `make langfanout` runs the Rust compile+run and compile-error cases green against real Redis+Docker.
- Plan diff confined to languages/rust-1.83/ + internal/worker/langfanout_rust_test.go.
</verification>

<success_criteria>
- LANG-06 proven end-to-end: rustc -O compile then produced-binary run, plus a compile-error path streaming compiler stderr + non-zero result.
- Zero core changes — adding Rust touched only its folder + one test file.
</success_criteria>

<output>
Create `.planning/phases/06-language-fan-out/06-02-SUMMARY.md` when done.
</output>
