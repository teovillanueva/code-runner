---
phase: 05-statelessness-scale
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/admission.ts
  - apps/api/src/config.ts
  - apps/api/src/routes/execute.ts
  - apps/api/test/admission.test.ts
  - docs/scaling.md
  - README.md
  - packages/contract/src/index.ts
autonomous: true
requirements: [SCALE-03, SCALE-05, SCALE-01]
must_haves:
  truths:
    - "POST /v1/execute returns 429 with a clear 'at capacity, retry' message when LLEN(jobs:queue) exceeds MAX_QUEUE_DEPTH — work is rejected, never silently dropped"
    - "Under capacity, POST /v1/execute still returns 202 and enqueues as before (no regression to the start-handshake contract)"
    - "The job-admission 429 is DISTINCT from the existing per-job stdin rate-limit/pending-byte 429 (different route, different trigger)"
    - "docs/scaling.md documents the autoscaling-by-queue-depth + scale-to-zero model where the scaling unit is the worker NODE (hosts N sandboxes), the native-Redis-for-worker constraint (Upstash API-only), the fly-autoscaler LLEN example, and FlyMachinesRunner as v2"
    - "docker compose up --scale worker=2 starts two independent stateless workers and a stdin frame routes only to the owning worker"
  artifacts:
    - path: "apps/api/src/admission.ts"
      provides: "queue-depth admission gate (LLEN jobs:queue vs MAX_QUEUE_DEPTH) → 429"
      min_lines: 25
    - path: "docs/scaling.md"
      provides: "autoscale-by-queue-depth + scale-to-zero design per deploy target"
      contains: "fly-autoscaler"
    - path: "apps/api/test/admission.test.ts"
      provides: "test: over-depth → 429 with retry message; under-depth → 202"
      contains: "429"
  key_links:
    - from: "apps/api/src/routes/execute.ts"
      to: "apps/api/src/admission.ts"
      via: "admission check runs before enqueue; returns 429 on over-capacity"
      pattern: "admission|atCapacity|MAX_QUEUE_DEPTH"
    - from: "apps/api/src/admission.ts"
      to: "Redis"
      via: "LLEN keys.jobQueue compared to config.maxQueueDepth"
      pattern: "llen|LLEN"
---

<objective>
Add global backpressure and the scaling design: make POST /v1/execute return 429 with a clear retry message when the system is at capacity (queue depth exceeds MAX_QUEUE_DEPTH) instead of letting the queue grow unbounded or silently dropping work (SCALE-03), and document the autoscaling-by-queue-depth + scale-to-zero model where the scaling unit is the worker node, plus a smoke-validation that `docker compose up --scale worker=2` runs two stateless workers with correct stdin routing (SCALE-05, SCALE-01).

Purpose: Under load the API must shed admission cleanly (429), and operators must have an honest, deploy-target-specific scaling guide.
Output: An admission gate + config, a Vitest test proving the 429, docs/scaling.md, a README scaling section, and a pasted multi-worker smoke result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-statelessness-scale/05-CONTEXT.md
@.planning/research/STACK-API-CONTRACT-DEPLOY.md
@docker-compose.yml

<read_first>
- apps/api/src/routes/execute.ts — the POST /v1/execute handler: validates with ExecuteRequestSchema, resolves manifest, builds JobSpec, writes spec+status, LPUSHes keys.jobQueue, returns 202. The admission check goes AFTER validation+manifest-resolve but BEFORE the pipeline.exec() that enqueues — so a rejected request never writes spec/status. Uses getRedis() from ../redis.ts and keys from @code-runner/contract.
- apps/api/src/ratelimit.ts — the EXISTING per-job stdin 429 (frame rate + pending-byte cap on /stdin). The NEW admission 429 is on /execute and is queue-depth-based — keep them clearly separate (distinct file admission.ts, distinct message). Reuse the same 429 status + JSON error shape for consistency.
- apps/api/src/config.ts — Config interface + loadConfig() from env (requireEnv pattern, parseInt with defaults). Add maxQueueDepth (env MAX_QUEUE_DEPTH, default 256). The singleton `config` is imported across the app.
- apps/api/src/redis.ts — getRedis() returns an ioredis client with llen() available.
- packages/contract/src/index.ts — keys.jobQueue = "jobs:queue"; this is the list to LLEN. (After 05-01, capacityFree may also exist here — if present, the admission check MAY also consult it as a secondary signal, but queue depth is the authoritative MVP gate; keep it simple.)
- apps/api/test/execute.test.ts — Vitest harness pattern: getApp() (resetManifests + makeApp), postExecute(app, body, token), getTestRedis() flushing between tests, VALID_TOKEN/REDIS_URL from vitest.config.ts env. Mirror this exactly for admission.test.ts. Tests run via `pnpm --filter @code-runner/api test` / `npm test` (vitest run) against a live test Redis.
- docker-compose.yml — the worker service definition; confirm it can scale (no host-port collisions on the worker; it only needs the docker socket + redis + soketi). The smoke test runs `docker compose up --scale worker=2`.
- .planning/research/STACK-API-CONTRACT-DEPLOY.md §3.4 — fly-autoscaler v0.3.1, FAS_CREATED_MACHINE_COUNT = "min(50, qdepth/2)" mapping Redis LLEN jobs → worker count; will NOT scale to zero (warm floor ≥1); pair with Machines auto-stop for true zero. Upstash is API-only (CFG-04). FlyMachinesRunner = v2. Cite these accurately in docs/scaling.md.
</read_first>

<interfaces>
From apps/api/src/config.ts (extend):
- interface Config { ...; maxQueueDepth: number }  // env MAX_QUEUE_DEPTH default 256
From @code-runner/contract:
- keys.jobQueue = "jobs:queue"
From apps/api/src/redis.ts:
- getRedis(): Redis  // ioredis; getRedis().llen(key)
Existing 429 JSON shape (ratelimit.ts) to mirror:
- c.json({ error: "...clear message...", retryAfterMs?: number }, 429)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Job-admission 429 (queue-depth gate) + config + wire into /execute</name>
  <files>apps/api/src/admission.ts, apps/api/src/config.ts, apps/api/src/routes/execute.ts, apps/api/test/admission.test.ts, packages/contract/src/index.ts</files>
  <read_first>apps/api/src/routes/execute.ts, apps/api/src/config.ts, apps/api/src/ratelimit.ts, apps/api/test/execute.test.ts</read_first>
  <behavior>
    - When LLEN(jobs:queue) >= MAX_QUEUE_DEPTH, POST /v1/execute returns HTTP 429 with a JSON body containing a clear "at capacity / retry" error message; NO spec/status is written and the queue length does not increase.
    - When LLEN(jobs:queue) < MAX_QUEUE_DEPTH, POST /v1/execute returns 202 {jobId, channel, status:"queued"} exactly as today.
    - The admission 429 is on /execute (job admission); it is distinct from the /stdin rate-limit 429 in ratelimit.ts.
  </behavior>
  <action>
Add maxQueueDepth to apps/api/src/config.ts: in the Config interface add `maxQueueDepth: number;` and in loadConfig set `maxQueueDepth: parseInt(process.env["MAX_QUEUE_DEPTH"] ?? "256", 10)`.

Create apps/api/src/admission.ts exporting an async function `atCapacity(): Promise<boolean>` that reads getRedis().llen(keys.jobQueue) and returns true when depth >= config.maxQueueDepth. Optionally export a helper `admissionError(depth, cap)` returning the 429 JSON body shape `{ error: "Executor at capacity (queue depth N ≥ cap M). Retry shortly.", retryAfterMs: <a small backoff like 1000> }`. Keep it dependency-light (reuse getRedis + config + keys). If 05-01's capacityFree counter exists, you MAY additionally treat free-slots <= 0 as at-capacity, but queue depth is the authoritative gate — document the choice in a comment.

In apps/api/src/routes/execute.ts: AFTER the manifest resolves successfully and BEFORE building the spec / running the enqueue pipeline, call `if (await atCapacity()) return c.json(admissionError(...), 429);`. Ensure a rejected request performs NO writes (the pipeline must not run). Keep the existing 202 path unchanged otherwise.

In packages/contract/src/index.ts: ensure `keys.jobQueue` is exported (it already is). No new key needed here unless 05-01 added capacityFree — if 05-01 added it, leave it; do not duplicate.

Create apps/api/test/admission.test.ts mirroring execute.test.ts's harness (getApp, postExecute, getTestRedis, VALID_TOKEN/REDIS_URL, flush between tests). 
  - Test 1 (over capacity): pre-seed Redis so LLEN(jobs:queue) >= MAX_QUEUE_DEPTH — to keep the test fast, temporarily set a low cap. The cleanest approach: drive the cap via env in vitest by reading config.maxQueueDepth, then RPUSH/LPUSH that many dummy ids, OR (preferred) make the test set MAX_QUEUE_DEPTH low before importing config — but config is a loaded singleton. So instead: LPUSH config.maxQueueDepth dummy jobIds onto keys.jobQueue, then postExecute a valid python body and assert status === 429 and the JSON error message mentions "capacity". Assert LLEN did NOT grow beyond the seeded count + 0 (no enqueue happened) and that no job:<id>:spec was written for the rejected request.
  - Test 2 (under capacity): flush the queue, postExecute a valid python body, assert 202 and a jobId is returned and LLEN(jobs:queue) increased by 1.
Flush keys.jobQueue (and dummy spec keys) in beforeEach.
  </action>
  <verify>
    <automated>cd /Users/teovillanueva/code-runner/apps/api && npm run typecheck && npm test -- admission</automated>
  </verify>
  <acceptance_criteria>
- `npm run typecheck` (tsc --noEmit) passes for apps/api.
- apps/api/src/admission.ts exists and reads LLEN of keys.jobQueue (`grep -qi "llen" apps/api/src/admission.ts`).
- config.ts has maxQueueDepth from MAX_QUEUE_DEPTH (`grep -q "maxQueueDepth" apps/api/src/config.ts && grep -q "MAX_QUEUE_DEPTH" apps/api/src/config.ts`).
- execute.ts calls the admission gate before enqueue (`grep -q "atCapacity\|admission" apps/api/src/routes/execute.ts`).
- EXECUTOR MUST RUN the Vitest admission test against a live test Redis and paste output: `npm test -- admission` (from apps/api). Output MUST show the over-capacity→429 and under-capacity→202 cases PASS.
  </acceptance_criteria>
  <done>POST /v1/execute returns a clear 429 at queue-depth capacity and 202 below it, proven by a passing Vitest test against live Redis; distinct from the stdin rate-limit 429.</done>
</task>

<task type="auto">
  <name>Task 2: docs/scaling.md + README scaling section + multi-worker smoke validation</name>
  <files>docs/scaling.md, README.md</files>
  <read_first>.planning/research/STACK-API-CONTRACT-DEPLOY.md, .planning/PROJECT.md, docker-compose.yml, docs/redis-constraint.md</read_first>
  <action>
Create docs/scaling.md documenting the scaling model honestly and per deploy target:
  - Scaling unit = the worker NODE: each worker is a long-lived process that launches its sandboxes INTERNALLY and hosts up to WORKER_MAX_SANDBOXES concurrent ones. It is NOT a microVM per execution. FlyMachinesRunner (Firecracker microVM per execution via the Fly Machines API) is a v2 option, noted with its latency/streaming trade-offs (cite PROJECT.md Key Decisions + research §3 decision note).
  - Statelessness (SCALE-01): API and workers are stateless, coupled only via Redis + soketi; each worker has an ephemeral workerId; run N replicas with `docker compose up --scale worker=2` (dev) or N Machines (prod).
  - Capacity & backpressure (SCALE-02/03): per-node slot cap (acquire-before-claim); global admission 429 when LLEN(jobs:queue) ≥ MAX_QUEUE_DEPTH — clients retry, work is never silently dropped.
  - Dead-worker reaper (SCALE-04): heartbeat key + owned-jobs set + label-based reaper; reference internal/reaper.
  - Autoscaling by queue depth (SCALE-05): the fleet scales UP when LLEN(jobs:queue) grows and toward zero when idle. Document fly-autoscaler v0.3.1 with the example `FAS_CREATED_MACHINE_COUNT = "min(50, qdepth/2)"` mapping Redis LLEN jobs → worker count, the ~15s reconcile loop, and the honest caveat that fly-autoscaler keeps a warm floor of ≥1 (pair with Machines auto-stop/auto-start for true scale-to-zero). The metric source is a documented `redis-cli LLEN jobs:queue` (or a small exporter).
  - Native-Redis-for-worker constraint (CFG-04): the worker needs native TCP pub/sub + blocking ops (SUBSCRIBE/BRPOP); Upstash REST and similar API-only Redis are NOT worker-viable (they are fine for the API). Link docs/redis-constraint.md.
  Keep claims honest — do NOT claim per-execution microVMs in v1.

In README.md add a "Scaling & Statelessness" section (or extend an existing deployment section) with a 3-5 line summary and a link to docs/scaling.md, plus the one-liner `docker compose up --scale worker=2` for local multi-worker.

SMOKE VALIDATION (SCALE-01 multi-worker routing): bring up the stack with two workers and confirm an interactive stdin frame reaches only the OWNING worker (ownership-by-subscription). Use the existing e2e harness: `make up` then `docker compose up -d --scale worker=2` (or `docker compose up --scale worker=2 -d`), run `scripts/e2e.sh` (or the documented demo) once, and confirm a successful interactive execute completes (the API enqueues, one of the two workers claims it, stdin routes to that worker, result returns). If the full e2e is not runnable in the exec environment, at minimum start `--scale worker=2`, assert both worker containers are Up and each logged a DISTINCT ephemeral workerId at boot (grep the logs), and paste that evidence. Paste the real command output (worker count + distinct workerIds and/or a successful e2e result) into the SUMMARY. Tear down with `make down` after.
  </action>
  <verify>
    <automated>test -f /Users/teovillanueva/code-runner/docs/scaling.md && grep -q "fly-autoscaler" /Users/teovillanueva/code-runner/docs/scaling.md && grep -q "scale-to-zero\|scale to zero" /Users/teovillanueva/code-runner/docs/scaling.md && grep -qi "scal" /Users/teovillanueva/code-runner/README.md</automated>
  </verify>
  <acceptance_criteria>
- docs/scaling.md exists and covers: scaling unit = worker node (not microVM-per-execution), FlyMachinesRunner = v2, fly-autoscaler LLEN example, scale-to-zero warm-floor caveat, native-Redis constraint (Upstash API-only). (`grep -q "worker node\|worker NODE" docs/scaling.md && grep -q "fly-autoscaler" docs/scaling.md && grep -qi "upstash" docs/scaling.md && grep -qi "v2" docs/scaling.md`).
- README.md has a scaling section linking docs/scaling.md and the `--scale worker=2` one-liner (`grep -qi "scaling" README.md && grep -q "scale worker=2" README.md`).
- EXECUTOR MUST RUN the multi-worker smoke and paste real output in the SUMMARY: either a successful `docker compose up --scale worker=2` e2e run OR evidence of two Up worker containers with DISTINCT ephemeral workerIds in their boot logs. State which in the SUMMARY.
  </acceptance_criteria>
  <done>docs/scaling.md and a README section document the honest autoscale-by-queue-depth + scale-to-zero model under the native-Redis constraint; multi-worker `--scale worker=2` is smoke-validated with pasted evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream client → API /execute | untrusted job-admission requests cross here; the admission gate sheds load |
| API → Redis | API reads LLEN(jobs:queue) for the admission decision (read-only on the hot path) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Denial of Service | unbounded job queue | mitigate | LLEN(jobs:queue) ≥ MAX_QUEUE_DEPTH → 429 at /execute; the queue cannot grow without bound, and clients get a clear retry signal instead of a silent drop |
| T-05-08 | Information Disclosure | admission error leaking internals | accept | the 429 message reports only queue depth vs cap (operational, non-sensitive); no secrets, no internal hostnames |
| T-05-09 | Spoofing | unauthenticated capacity probing | mitigate | /v1/execute is already behind the constant-time EXECUTOR_API_TOKEN bearer (app.ts /v1/* middleware); the admission gate inherits that auth |
| T-05-SC | Tampering | npm installs | mitigate | no new npm dependency introduced; admission.ts uses the already-pinned ioredis + @code-runner/contract |
</threat_model>

<verification>
- `npm run typecheck` + `npm test -- admission` (apps/api) green against live test Redis; over-capacity→429, under-capacity→202.
- docs/scaling.md + README scaling section present and accurate (worker-node scaling unit, fly-autoscaler LLEN, scale-to-zero caveat, native-Redis constraint, FlyMachinesRunner v2).
- `docker compose up --scale worker=2` smoke evidence pasted (successful e2e or two Up workers with distinct ephemeral workerIds).
</verification>

<success_criteria>
- SCALE-03: full-capacity conditions surface as a clear 429 at /execute (no silent drops), distinct from the existing stdin 429.
- SCALE-05: the repo documents the autoscale-by-queue-depth + scale-to-zero model per deploy target with the worker-node scaling unit and native-Redis constraint; FlyMachinesRunner noted as v2.
- SCALE-01: `docker compose up --scale worker=2` runs two stateless workers with correct ownership-by-subscription stdin routing (smoke-validated).
</success_criteria>

<output>
Create `.planning/phases/05-statelessness-scale/05-03-SUMMARY.md` when done. Include the PASTED admission test output and the multi-worker smoke evidence.
</output>
