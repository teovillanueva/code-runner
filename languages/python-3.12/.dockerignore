# Keep the build context minimal — only the Dockerfile and the files it COPYs.
# Exclude everything that is not part of the image definition.
*
!Dockerfile
!sitecustomize.py
!zygote_agent.py
# zygote_selftest.py is a HOST-side test client — intentionally NOT baked in.
