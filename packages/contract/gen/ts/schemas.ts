// Code generated from schema/wire.schema.json by `pnpm contract`. DO NOT EDIT.

import { z } from "zod";

export const LimitsSchema = z.object({ "wallTimeMs": z.number().int().gte(1).describe("Max total lifetime of the session in ms. Kills unconditionally."), "idleMs": z.number().int().gte(1).describe("Max ms with no stdout and no stdin before the sandbox is killed."), "cpuMs": z.number().int().gte(1).describe("Max accumulated CPU time in ms (cgroup), independent of wall-clock."), "memoryMb": z.number().int().gte(1).describe("Memory cap in MiB. memory == memory-swap (no swap)."), "pids": z.number().int().gte(1).describe("Max number of processes/threads (pids-limit)."), "outputKb": z.number().int().gte(1).describe("Max combined stdout+stderr in KiB before truncation."), "maxArtifacts": z.number().int().gte(1).describe("Max number of captured artifact files before excess is dropped (artifactsTruncated=true).").default(20), "maxArtifactBytes": z.number().int().gte(1).describe("Max total bytes across all captured artifacts before excess is dropped (artifactsTruncated=true).").default(4194304) }).strict().describe("Resource limits for a sandbox. The three clocks (wall/idle/cpu) plus memory, pids and output caps.");

export const LimitsOverrideSchema = z.object({ "wallTimeMs": z.number().int().gte(1).optional(), "idleMs": z.number().int().gte(1).optional(), "cpuMs": z.number().int().gte(1).optional(), "memoryMb": z.number().int().gte(1).optional(), "pids": z.number().int().gte(1).optional(), "outputKb": z.number().int().gte(1).optional(), "maxArtifacts": z.number().int().gte(1).optional(), "maxArtifactBytes": z.number().int().gte(1).optional() }).strict().describe("Optional per-request override of a subset of Limits.");

export const FileInputSchema = z.object({ "name": z.string().min(1).max(255).describe("Relative path written into the sandbox workspace; may contain '/' subdirectories (e.g. data/input.csv). Must not escape the workspace; absolute paths and traversal are rejected."), "content": z.string().describe("File content. Interpreted per `encoding`: utf8 text by default, or base64-encoded arbitrary bytes when encoding=base64."), "encoding": z.enum(["utf8","base64"]).describe("How content is encoded. utf8=text (default, back-compat); base64=arbitrary bytes.").default("utf8") }).strict().describe("A single source file submitted by the caller.");

export const ManifestSchema = z.object({ "language": z.string().min(1), "version": z.string().min(1), "aliases": z.array(z.string()).default([]), "image": z.string().min(1).describe("Pre-built image with all libs baked in (no runtime dependency resolution)."), "entrypoint": z.string().min(1).describe("Main file name, e.g. main.py."), "compile": z.union([z.array(z.string()).describe("Optional compile command (argv). null for interpreted languages."), z.null().describe("Optional compile command (argv). null for interpreted languages.")]).describe("Optional compile command (argv). null for interpreted languages."), "run": z.array(z.string()).min(1).describe("Run command (argv)."), "interactive": z.boolean().describe("Whether the language supports a live interactive stdin session."), "preimport": z.union([z.array(z.string()).describe("Optional zygote pre-import set: importable module/package names the zygote parent loads once at warm time so forked children share their pages copy-on-write. A NON-EMPTY array opts the language into the ZygoteRunner tier (interpreted, heavy-import languages like Python/R); absent or empty routes the language to the DockerSocketRunner tier (compiled or no-import languages like Rust/SQLite). Names are passed verbatim to the language agent (Python: importlib.import_module; R: library())."), z.null().describe("Optional zygote pre-import set: importable module/package names the zygote parent loads once at warm time so forked children share their pages copy-on-write. A NON-EMPTY array opts the language into the ZygoteRunner tier (interpreted, heavy-import languages like Python/R); absent or empty routes the language to the DockerSocketRunner tier (compiled or no-import languages like Rust/SQLite). Names are passed verbatim to the language agent (Python: importlib.import_module; R: library()).")]).describe("Optional zygote pre-import set: importable module/package names the zygote parent loads once at warm time so forked children share their pages copy-on-write. A NON-EMPTY array opts the language into the ZygoteRunner tier (interpreted, heavy-import languages like Python/R); absent or empty routes the language to the DockerSocketRunner tier (compiled or no-import languages like Rust/SQLite). Names are passed verbatim to the language agent (Python: importlib.import_module; R: library()).").default(null), "defaultLimits": z.object({ "wallTimeMs": z.number().int().gte(1).describe("Max total lifetime of the session in ms. Kills unconditionally."), "idleMs": z.number().int().gte(1).describe("Max ms with no stdout and no stdin before the sandbox is killed."), "cpuMs": z.number().int().gte(1).describe("Max accumulated CPU time in ms (cgroup), independent of wall-clock."), "memoryMb": z.number().int().gte(1).describe("Memory cap in MiB. memory == memory-swap (no swap)."), "pids": z.number().int().gte(1).describe("Max number of processes/threads (pids-limit)."), "outputKb": z.number().int().gte(1).describe("Max combined stdout+stderr in KiB before truncation."), "maxArtifacts": z.number().int().gte(1).describe("Max number of captured artifact files before excess is dropped (artifactsTruncated=true).").default(20), "maxArtifactBytes": z.number().int().gte(1).describe("Max total bytes across all captured artifacts before excess is dropped (artifactsTruncated=true).").default(4194304) }).strict().describe("Resource limits for a sandbox. The three clocks (wall/idle/cpu) plus memory, pids and output caps.") }).strict().describe("A language package manifest (languages/<lang-version>/manifest.json).");

export const LanguageInfoSchema = z.object({ "language": z.string(), "version": z.string(), "aliases": z.array(z.string()), "interactive": z.boolean() }).strict().describe("Public language descriptor returned by GET /v1/languages.");

export const ExecuteRequestSchema = z.object({ "language": z.string().min(1).describe("Language name or alias."), "version": z.string().describe("Optional explicit version; if omitted the only/most-recent match is used.").optional(), "files": z.array(z.object({ "name": z.string().min(1).max(255).describe("Relative path written into the sandbox workspace; may contain '/' subdirectories (e.g. data/input.csv). Must not escape the workspace; absolute paths and traversal are rejected."), "content": z.string().describe("File content. Interpreted per `encoding`: utf8 text by default, or base64-encoded arbitrary bytes when encoding=base64."), "encoding": z.enum(["utf8","base64"]).describe("How content is encoded. utf8=text (default, back-compat); base64=arbitrary bytes.").default("utf8") }).strict().describe("A single source file submitted by the caller.")).min(1), "limits": z.object({ "wallTimeMs": z.number().int().gte(1).optional(), "idleMs": z.number().int().gte(1).optional(), "cpuMs": z.number().int().gte(1).optional(), "memoryMb": z.number().int().gte(1).optional(), "pids": z.number().int().gte(1).optional(), "outputKb": z.number().int().gte(1).optional(), "maxArtifacts": z.number().int().gte(1).optional(), "maxArtifactBytes": z.number().int().gte(1).optional() }).strict().describe("Optional per-request override of a subset of Limits.").optional(), "collectOutput": z.boolean().describe("Opt-in: when true, the worker accumulates stdout/stderr and captures workspace artifacts into a pullable RunResult (GET /v1/jobs/:id/output). Defaults to false.").optional() }).strict().describe("Body of POST /v1/execute.");

export const ExecuteResponseSchema = z.object({ "jobId": z.string(), "channel": z.string().describe("soketi channel the client must subscribe to (private-run-<jobId>)."), "status": z.literal("queued") }).strict().describe("202 response of POST /v1/execute.");

export const JobSpecSchema = z.object({ "jobId": z.string(), "channel": z.string(), "language": z.string(), "version": z.string(), "image": z.string(), "entrypoint": z.string(), "compile": z.union([z.array(z.string()), z.null()]), "run": z.array(z.string()).min(1), "interactive": z.boolean(), "files": z.array(z.object({ "name": z.string().min(1).max(255).describe("Relative path written into the sandbox workspace; may contain '/' subdirectories (e.g. data/input.csv). Must not escape the workspace; absolute paths and traversal are rejected."), "content": z.string().describe("File content. Interpreted per `encoding`: utf8 text by default, or base64-encoded arbitrary bytes when encoding=base64."), "encoding": z.enum(["utf8","base64"]).describe("How content is encoded. utf8=text (default, back-compat); base64=arbitrary bytes.").default("utf8") }).strict().describe("A single source file submitted by the caller.")).min(1), "limits": z.object({ "wallTimeMs": z.number().int().gte(1).describe("Max total lifetime of the session in ms. Kills unconditionally."), "idleMs": z.number().int().gte(1).describe("Max ms with no stdout and no stdin before the sandbox is killed."), "cpuMs": z.number().int().gte(1).describe("Max accumulated CPU time in ms (cgroup), independent of wall-clock."), "memoryMb": z.number().int().gte(1).describe("Memory cap in MiB. memory == memory-swap (no swap)."), "pids": z.number().int().gte(1).describe("Max number of processes/threads (pids-limit)."), "outputKb": z.number().int().gte(1).describe("Max combined stdout+stderr in KiB before truncation."), "maxArtifacts": z.number().int().gte(1).describe("Max number of captured artifact files before excess is dropped (artifactsTruncated=true).").default(20), "maxArtifactBytes": z.number().int().gte(1).describe("Max total bytes across all captured artifacts before excess is dropped (artifactsTruncated=true).").default(4194304) }).strict().describe("Resource limits for a sandbox. The three clocks (wall/idle/cpu) plus memory, pids and output caps."), "enqueuedAtMs": z.number().int().describe("Unix epoch ms when the API enqueued the job."), "collectOutput": z.boolean().describe("Resolved opt-in flag from ExecuteRequest; when true the worker persists a RunResult and captures artifacts. The API always writes an explicit boolean (default false).").optional(), "traceparent": z.string().describe("W3C trace-context header for cross-seam trace correlation (optional).").optional(), "tracestate": z.string().describe("W3C tracestate header (optional).").optional() }).strict().describe("The fully-resolved job the API enqueues for the worker. The API resolves the manifest so the worker stays language-agnostic at runtime.");

export const JobStateSchema = z.enum(["queued","starting","running","done","killed","error"]).describe("Lifecycle state of a job.");

export const JobStatusSchema = z.object({ "jobId": z.string(), "channel": z.string(), "language": z.string(), "version": z.string(), "state": z.enum(["queued","starting","running","done","killed","error"]).describe("Lifecycle state of a job."), "updatedAtMs": z.number().int() }).strict().describe("Response of GET /v1/jobs/:id.");

export const StdinMessageSchema = z.object({ "chunk": z.string().describe("Bytes to write to stdin (UTF-8).") }).strict().describe("Published on stdin:<jobId>. Carries a chunk to write to the process stdin.");

export const ControlTypeSchema = z.enum(["start","kill","stdin_close"]);

export const ControlMessageSchema = z.object({ "type": z.enum(["start","kill","stdin_close"]) }).strict().describe("Published on ctrl:<jobId>. Routes lifecycle control to the owning worker.");

export const StagePhaseSchema = z.enum(["queued","compiling","running"]);

export const StageEventSchema = z.object({ "phase": z.enum(["queued","compiling","running"]) }).strict().describe("soketi event 'stage' on private-run-<jobId>.");

export const OutputChunkEventSchema = z.object({ "chunk": z.string(), "seq": z.number().int().describe("Monotonic sequence number for ordering.") }).strict().describe("soketi events 'stdout' and 'stderr' on private-run-<jobId>.");

export const ResultEventSchema = z.object({ "exitCode": z.union([z.number().int(), z.null()]), "signal": z.union([z.string(), z.null()]), "timedOut": z.boolean().describe("Killed by the wall-clock or cpu clock."), "idleTimedOut": z.boolean().describe("Killed by the idle clock."), "truncated": z.boolean().describe("Output exceeded outputKb and was truncated."), "durationMs": z.number().int() }).strict().describe("Terminal soketi event 'result' on private-run-<jobId>.");

export const ArtifactSchema = z.object({ "name": z.string().describe("Relative file name as written by the program into its working directory (e.g. plot.png)."), "mimeType": z.string().describe("Best-effort detected MIME type of the captured file."), "bytes": z.number().int().gte(0).describe("Size of the captured file in bytes."), "url": z.string().describe("Presigned GET URL the consumer/browser fetches directly (no bearer).") }).strict().describe("A single file captured from the sandbox working directory, referenced by an object-storage presigned URL. URL-only (no inline bytes): object storage is the single backend.");

export const CompileResultSchema = z.object({ "exitCode": z.union([z.number().int().describe("Exit code of the compile command. Non-zero means compilation failed and the run stage did not execute."), z.null().describe("Exit code of the compile command. Non-zero means compilation failed and the run stage did not execute.")]).describe("Exit code of the compile command. Non-zero means compilation failed and the run stage did not execute."), "signal": z.union([z.string().describe("Signal that terminated the compile command, if any."), z.null().describe("Signal that terminated the compile command, if any.")]).describe("Signal that terminated the compile command, if any."), "stdout": z.string().describe("Accumulated stdout of the compile command."), "stderr": z.string().describe("Accumulated stderr of the compile command (compiler diagnostics)."), "output": z.string().describe("Interleaved stdout+stderr of the compile stage, in emission order (mirrors Piston's compile.output)."), "durationMs": z.number().int().describe("Wall-clock time of the compile stage in ms.") }).strict().describe("Result of the compile stage for compiled languages (Piston-style separate `compile` object). Present in RunResult.compile only when a compile step actually ran. Compiler diagnostics conventionally go to stderr; a non-zero exitCode means compilation failed and the run stage did NOT execute.");

export const RunResultSchema = z.object({ "exitCode": z.union([z.number().int(), z.null()]), "signal": z.union([z.string(), z.null()]), "timedOut": z.boolean().describe("Killed by the wall-clock or cpu clock."), "idleTimedOut": z.boolean().describe("Killed by the idle clock."), "truncated": z.boolean().describe("Output exceeded outputKb and was truncated."), "durationMs": z.number().int(), "stdout": z.string().describe("Accumulated stdout of the RUN stage (within the outputKb budget; same bytes streamed to soketi). Compile-stage output lives in `compile`, not here."), "stderr": z.string().describe("Accumulated stderr of the RUN stage (within the outputKb budget; same bytes streamed to soketi). Compile-stage diagnostics live in `compile.stderr`, not here."), "compile": z.object({ "exitCode": z.union([z.number().int().describe("Exit code of the compile command. Non-zero means compilation failed and the run stage did not execute."), z.null().describe("Exit code of the compile command. Non-zero means compilation failed and the run stage did not execute.")]).describe("Exit code of the compile command. Non-zero means compilation failed and the run stage did not execute."), "signal": z.union([z.string().describe("Signal that terminated the compile command, if any."), z.null().describe("Signal that terminated the compile command, if any.")]).describe("Signal that terminated the compile command, if any."), "stdout": z.string().describe("Accumulated stdout of the compile command."), "stderr": z.string().describe("Accumulated stderr of the compile command (compiler diagnostics)."), "output": z.string().describe("Interleaved stdout+stderr of the compile stage, in emission order (mirrors Piston's compile.output)."), "durationMs": z.number().int().describe("Wall-clock time of the compile stage in ms.") }).strict().describe("Result of the compile stage for compiled languages (Piston-style separate `compile` object). Present in RunResult.compile only when a compile step actually ran. Compiler diagnostics conventionally go to stderr; a non-zero exitCode means compilation failed and the run stage did NOT execute.").optional(), "artifacts": z.array(z.object({ "name": z.string().describe("Relative file name as written by the program into its working directory (e.g. plot.png)."), "mimeType": z.string().describe("Best-effort detected MIME type of the captured file."), "bytes": z.number().int().gte(0).describe("Size of the captured file in bytes."), "url": z.string().describe("Presigned GET URL the consumer/browser fetches directly (no bearer).") }).strict().describe("A single file captured from the sandbox working directory, referenced by an object-storage presigned URL. URL-only (no inline bytes): object storage is the single backend.")).describe("Captured workspace artifacts, each referenced by presigned URL."), "artifactsTruncated": z.boolean().describe("True when captured files exceeded maxArtifacts/maxArtifactBytes and excess was dropped.") }).strict().describe("The persisted, pullable result of a collected run (GET /v1/jobs/:id/output). ResultEvent's terminal fields plus accumulated stdout/stderr and captured artifacts.");
